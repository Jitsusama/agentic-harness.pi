# Git Commit Convention

Teaches the agent when and how to commit: commit frequency,
atomic commits and history management.

For the commit message format itself (conventional commits
spec), see `commit-format`. This skill covers the operational
conventions around committing.
