2ed2168dcde146783e344dcd5dff93ba3ea493d3|2026-03-22 18:19:08 -0400|Fix Shift+Escape Redirect in Extension UIs (#75)
* feat(skills): add stacked PR convention skill

Covers the full lifecycle of stacked PRs: detecting
whether a PR is in a stack, verifying base chains before
merging, merge order (always bottom-up), retargeting
dependents, branch deletion safety and rebasing stacked
branches. Triggered when merging PRs, rebasing branches
with dependents or creating PRs based on other PRs.

Born from a session where merging a stack without
verifying the base chain auto-closed dependent PRs and
required recreating them.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

* fix(ui): work around pi-tui shift+escape bug

pi-tui's matchesKey rejects all modifiers on escape
(if modifier !== 0 return false), so
matchesKey(data, Key.shift("escape")) always returns
false. This broke the Shift+Escape redirect in all
three prompt types.

Add an isShiftEscape helper that uses parseKey instead,
which correctly identifies Shift+Escape from Kitty
CSI-u and modifyOtherKeys sequences.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---------

Co-authored-by: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>
---COMMIT_END---
6c4b27572abb0a386b72e095723f3f7847a50161|2026-03-22 18:18:38 -0400|feat(skills): add stacked PR convention skill (#74)
Covers the full lifecycle of stacked PRs: detecting
whether a PR is in a stack, verifying base chains before
merging, merge order (always bottom-up), retargeting
dependents, branch deletion safety and rebasing stacked
branches. Triggered when merging PRs, rebasing branches
with dependents or creating PRs based on other PRs.

Born from a session where merging a stack without
verifying the base chain auto-closed dependent PRs and
required recreating them.

Co-authored-by: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>
---COMMIT_END---
796a806d004227289c661d7d0658c2db3b3cb3c3|2026-03-22 17:30:48 -0400|Reapply "Merge pull request #65 from Jitsusama/jitsusama/ddd-clean-design-phase3"
This reverts commit 353d7a087e3e456613dc8c28a8856f2f9d694d66.

---COMMIT_END---
32ba57681b4525759a4488a6ccb46991ab810601|2026-03-22 17:09:38 -0400|docs: acknowledge interceptor mutation site
AGENTS.md now recognises interceptors as a second
sanctioned site for mutating event.input.command,
alongside registerGuardian. The attribution interceptor's
JSDoc cross-references this rule with an explanation of
why it mutates the command directly.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
dcfa732d8f497b5ad4be777d11d711351a7e3373|2026-03-22 17:08:16 -0400|style: rewrite fragment comments as sentences
Inline comments that were verb-phrase fragments missing
subjects now read as complete sentences. Labels and
category markers within arrays are left as-is per the
style standard.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
1c96635e1f7490aa84ff4663aefce566412430c2|2026-03-22 16:58:47 -0400|docs: add missing JSDoc to all exports
Every exported function, interface, type and constant
across extensions/ now has a JSDoc comment describing
what it does. The gaps were spread across guardians,
lib/ui types, Google Workspace handlers, and web search
modules.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
a22110cf00df58f098a1cd976fbb4f998d02b674|2026-03-22 15:47:17 -0400|refactor(pr-reply): use shared languageFromPath
code-context.ts maintained its own LANGUAGE_BY_EXTENSION
map for syntax highlighting. Replaced with languageFromPath
from lib/ui/content-renderer.ts, which wraps the same
Pi utility that three other consumers already use.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
7ff4fbd61cd52f660a3ed7cfda3c3946179c3d10|2026-03-22 15:43:20 -0400|fix(pr-review): rename handled to passed in lifecycle
The TabState interface in model.ts already used `passed`
but lifecycle.ts still referenced `handled` — a stale name
from before the rename. This caused val.handled to read
undefined from TabState (which only has `passed`), so the
tab count in the status line was always 0.

Now lifecycle uses `passed` for serialization, status
display and deserialization. The restore path falls back
to `handled` for sessions persisted before this rename.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
c82831257daf89c5be47d6903014f93b7267de0b|2026-03-22 15:40:24 -0400|perf(status-line): event-driven cost tracking
The status line was scanning the entire session branch on
every render (every keypress, scroll, resize) to compute
the total cost. For long sessions this could become slow.

Now the cost is computed once at session start from the
existing branch, then accumulated incrementally via the
message_end event. The render function reads the cached
total instead of scanning.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
d98d5b3cb71b61629ee07f3a43ce5722bc61ea00|2026-03-22 15:31:59 -0400|refactor(google): replace router switch with registry
The 20-case switch in router.ts is now a Map<string,
ActionHandler> registry. Each action maps to its handler
function directly. The routeAction function looks up the
handler and calls it, falling back to an "unknown action"
response.

Only handleListSharedDrives needs a wrapper because it
takes auth alone rather than the standard (params, auth,
ctx) signature.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
558a08e61d62302e7cf77f3fe242050a7c9199bd|2026-03-22 15:30:56 -0400|refactor(briefing): separate domain data from LLM coaching
Both PR workflow briefing files mixed domain summaries
with LLM instruction strings. Each briefing function is
now split into a *Summary function (domain data only)
and the original convenience wrapper that appends LLM
coaching. Callers that need just data can import the
summary; callers that want the full text keep using the
existing wrapper.

Also extracted per-section helpers in pr-review briefing
(formatReviewers, formatDescription, formatLinkedIssues,
formatReferences, formatSourceFiles, formatDiff) to
replace section-divider comments inside activationBriefing.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
4c7233c066306fd5f57e41b575e8cd6639aa49fc|2026-03-22 15:27:05 -0400|refactor(ui): extract prompt controllers from closures
Each prompt variant (single, tabbed, workspace) had its
entire implementation inside an anonymous ctx.ui.custom
callback. The mutable state and handlers are now in named
factory functions: createSingleController,
createTabbedController, createWorkspaceController.

The show*Prompt exports become thin wrappers that pass
dependencies to the factory. Dependencies (tui, theme,
done) are explicit in the factory signature rather than
implicitly captured from the callback scope.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
0976c2f27fbfb914be6d56e990fa27f48d00d008|2026-03-22 15:18:46 -0400|refactor(guardian): extract markdown entity review
PR and issue guardians had structurally identical review
flows that only differed in label strings. The shared
concept — "present a markdown body with title for human
review" — now lives in lib/guardian/review-entity.ts.

Each guardian still owns detect/parse/review per AGENTS.md;
they delegate the review interaction to the shared helper
with entity-specific config.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
21aeb3a86662ad3b907a69bd813125e62a023b69|2026-03-22 15:09:34 -0400|refactor(web-search): type-guard result.details
Replace four `as` casts on unknown result.details with
ReaderDetails interface and isReaderDetails type guard.
Add hasErrorDetails for the error path.

The renderResult functions now validate the shape before
accessing fields instead of blindly casting.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
c7bf870568a54850630b7306dcdfe7ff9d66368a|2026-03-22 15:07:57 -0400|refactor(ui): rename scrollToLine to scrollToContentLine
The function adjusts vOffset by content line number, but
the old name suggested it accepted item indices. The
rename makes the abstraction level honest.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
368349ebb8a84f4d179c8ec68a66b1879b12ae75|2026-03-22 15:02:10 -0400|refactor(ui): extract tab-completion helper
New tabCompletion() in lib/ui/tab-completion.ts replaces
the identical tabStatus/allComplete callback boilerplate
in pr-annotate, pr-reply, and pr-review workspaces.

Accepts a tab ID list, a predicate, and an optional
completable subset so summary tabs can be excluded from
allComplete without special-casing in each consumer.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
3569cee4a578d7b9d0fc1f7df79710a2380f316d|2026-03-22 14:54:04 -0400|refactor(state): define PersistedState in all workflows
Replace inline type arguments to getLastEntry with named
PersistedState interfaces. plan-workflow, tdd-workflow,
and pr-reply-workflow now match the pattern pr-review
already uses.

Each interface documents the exact shape written to
session history, separate from the runtime state type.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
e9d3afcfb8eeb8397c70a7d865124e159a2d714d|2026-03-22 14:49:43 -0400|refactor(review-post): unify review comment API
Replace ReviewAPIComment (snake_case wire format) with
ReviewComment (camelCase domain type). postReview now
handles the transformation to GitHub's wire format
internally, so consumers never see snake_case fields.

Both pr-annotate-workflow and pr-review-workflow now pass
the same ReviewComment type to the shared posting module.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
4e0c4301afb08437b8f5b7d160e48c4d393607c8|2026-03-22 14:47:06 -0400|refactor(graphql): accept generic variables
runGraphQL now takes Record<string, string | number>
instead of a PRReference. Callers pass their own variable
maps, so any query shape works — not just PR queries.

gather.ts no longer bypasses the shared runner with a
manual gh api graphql call for issue queries.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
1a899f1603a43b1ec6f7cc127145faf7572e9be0|2026-03-22 14:40:18 -0400|style(google): move helpers after exports
Seven private email-parsing helpers (parseEmailAddress,
parseEmailAddresses, hasAttachments, extractBody,
extractAttachments, decodeBase64, stripHtml) were between
getThread and sendEmail, splitting the public API in half.

All private helpers are now after the last public export
(markUnread), so the public API reads as a continuous
block.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
221a3b72febe7082e7a888901f36ba9f099d0e2f|2026-03-22 14:38:03 -0400|style(pr-reply): move helpers after exports
Five private helpers (plainTextResponse, isRecommendation,
currentThread, findNextPendingInReview, VALID_RECOMMENDATIONS)
were before all public handler functions. Three others
(applyThreadChoice, reviewAndPostReply, handleRepoResult)
were already at the bottom.

All private helpers are now after all public exports. Also
removed a duplicate JSDoc comment on currentThread.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
869b74c006e4bdd98e23ccf232f8accd88116053|2026-03-22 14:36:24 -0400|style(pr-review): move helpers after exports
Private helpers (plainTextResponse, resolvePR,
ensureContext, isReviewVerdict) were interleaved before
public handler functions. Readers had to skip past
infrastructure to find the public API.

Helpers are now after all public exports, ordered by
first call site: plainTextResponse and resolvePR
(handleActivate), ensureContext (handleOverview), then
isReviewVerdict (handleSubmit).

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
21fc460eb8c85cb4f0df026f17190d8d3ec41d68|2026-03-22 14:18:57 -0400|refactor(google): rename oauth getCredentials
Becomes extractTokens to avoid confusion with the
identically named getCredentials in credentials.ts. The
oauth version extracts raw token credentials from an
OAuth2 client; the credentials version looks up stored
account credentials. SCOPES stays exported since
dual-flow.ts imports it within the same auth module.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
79b90770e58138b1829345a4bceb39a6ee0d8463|2026-03-22 14:16:49 -0400|refactor(pr): rename Review and Reviewer types
In pr-reply-workflow, Review becomes ReceivedReview to
distinguish the incoming GitHub review from the act of
reviewing. In pr-review-workflow, Reviewer becomes
ReviewerStatus since it captures a reviewer's verdict
state, not the reviewer as a person.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
b133a4dc14af7ed8d3e7ea40fbc76f6e97af0a9c|2026-03-22 14:14:00 -0400|refactor(pr-reply): rename analysis and replies files
analysis.ts becomes thread-context.ts since it builds
structured context for each review thread. replies.ts
becomes reply-guidance.ts since it produces guidance
for composing replies.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
91bf3a972feb53c198b56ec8da30a7eee59c5988|2026-03-22 14:13:12 -0400|refactor(ui): rename text.ts to text-layout.ts
The vague name didn't communicate what the module does.
text-layout.ts describes its actual responsibility: text
wrapping, indentation, and content width calculations.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
535c7bffa00babeed3fb59b3f47f6ecc3b2fd38f|2026-03-22 14:11:56 -0400|refactor(history-guardian): rename confirm to review
Aligns with the guardian file naming convention where the
review step lives in review.ts, matching pr-guardian,
issue-guardian and commit-guardian.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
61d8edb521a246455068e7e0c6adf1fee4525e51|2026-03-22 14:10:42 -0400|refactor(ui): rename ContentFn to ContentRenderer
ContentFn named the mechanism (it's a function).
ContentRenderer names the role (it renders content).
AsyncContentFn becomes AsyncContentRenderer for
consistency.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
b03a8cc343182e46f15e481492af6f182f0717e0|2026-03-22 14:10:01 -0400|refactor(ui): rename Action and Option types
Action becomes KeyAction since it represents a keyboard
shortcut action (key + label). Option becomes ListChoice
since it represents a selectable item in a numbered list.
The old names were near-synonyms that didn't distinguish
their roles in the prompt system.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
d00b39662e5b4608698dde4b44cba9c6f3bedffe|2026-03-22 14:08:14 -0400|refactor(ui): rename WorkspaceConfig
Becomes WorkspacePromptConfig to follow the existing
convention (SinglePromptConfig, TabbedPromptConfig).

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
66a88d2bb50ea753f5ae9689cf7f6c4bd39768f5|2026-03-22 14:06:34 -0400|refactor(pr-annotate): rename vet to review
vetComments becomes reviewProposedComments and vet.ts
becomes review.ts. "Vet" was informal jargon; "review
proposed comments" describes the user-facing action. The
local vetComments variable becomes trackedComments to
avoid confusion with the function name.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
a5dfcea76f10ba785c1b73cd0fb2666e2e25604d|2026-03-22 14:01:59 -0400|refactor(pr): rename textResult to plainTextResponse
Descriptive name for the helper that wraps a string in
the MCP tool result format. Both PR workflows had
identical helpers; both renamed.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
14ebe8befa218a2bf1190fe6dc33107ba3e71c17|2026-03-22 14:01:08 -0400|refactor(pr): rename brief* to *Briefing
The verb form (briefActivation, briefThread) read as
adjectives. The noun form (activationBriefing,
threadBriefing) makes each function's role obvious: it
produces a briefing document for a specific context.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
43b8b4cb0c3b84986aa25c93952f4cb6b4f57995|2026-03-22 13:59:39 -0400|refactor(pr): rename context builders and filters
buildPRReviewContext becomes injectReviewGuidance and
buildPRReplyContext becomes injectReplyGuidance since
they inject LLM steering instructions, not generic
context. The context filters become
pruneStaleReviewGuidance and pruneStaleReplyGuidance
to describe what they actually do: remove outdated
guidance messages from the conversation.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
db110f5dc8383fafe2c5e78fa8c4bdb5927d2229|2026-03-22 13:58:34 -0400|refactor(plan): rename handlePlanWritten
Becomes offerImplementationTransition to name the purpose
(offering a transition to implementation mode) rather than
the trigger (a plan file being written).

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
f1deadfd24ab6c275708ae2807635f170c2c62c3|2026-03-22 13:57:35 -0400|refactor(pr-review): rename HandlerDeps to ReviewContext
The DI container name was technical. Consumers think of
it as the review context: the state and API surface
needed to handle each review action.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
d456cd6801bfcaae701aa33e181a6a434a7fa741|2026-03-22 13:49:13 -0400|refactor(pr-review): rename crawl to gatherContext
CrawlResult becomes PRContext since consumers think about
PR context, not the crawling mechanism. The crawl()
function becomes gatherContext() and crawler.ts becomes
gather.ts. Internal implementation names (CrawlConfig,
crawlIssueDeeply) stay unchanged since they describe the
private recursive strategy accurately.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
e19d13560c341fe7bfc85d5a7dceeac230035a89|2026-03-22 13:45:36 -0400|refactor(tdd): rename Phase to TddPhase
The unqualified Phase type collided with ReviewPhase in
the PR review workflow. TddPhase makes the domain
explicit and follows the same pattern.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
18590856fb8567b611232b3148f52ea609d31372|2026-03-22 13:42:38 -0400|refactor(pr-reply): rename Thread and Comment types
Thread becomes ReviewThread to avoid collision with the
generic concept of threads (email, OS). Comment becomes
ThreadComment to distinguish from other comment types
across the codebase (ProposedComment, ReviewObservation,
ReviewAPIComment). String literals and JSDoc prose kept
their original wording since the domain concept is still
"thread" — only the TypeScript interface names changed.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
5a228501e576cbb5e76920bb366e29b376b039c0|2026-03-22 13:35:15 -0400|refactor: disambiguate ReviewComment names
Three interfaces shared the name ReviewComment across
extensions, making cross-reference confusing. The annotate
workflow's version becomes ProposedComment (a comment
proposed by the LLM for self-review). The review
workflow's version becomes ReviewObservation (a comment
with lifecycle status during a structured code review).
The wire format ReviewAPIComment was already distinct.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
353d7a087e3e456613dc8c28a8856f2f9d694d66|2026-03-22 09:46:17 -0400|Revert "Merge pull request #65 from Jitsusama/jitsusama/ddd-clean-design-phase3"
This reverts commit 35047999f903635a4cea9ea2f480033f7ee34122, reversing
changes made to a072b983cd01d508df8a29d6b8321abf75acd2ae.

---COMMIT_END---
d1b56e83a510e6268abf1fa36486059713ed90a5|2026-03-22 09:37:11 -0400|refactor(tdd): extract phase handlers to dispatch map
The ~100-line if-chain in index.ts becomes a dispatch map
in handlers.ts with one named function per action (handleStart,
handleStop, handleRed, handleGreen, handleRefactor, handleDone).

The shared gate-then-advance pattern is visible in each handler.
index.ts delegates with a single dispatchPhaseAction call.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
53d8591f2ea0f8b0463ac7a7b13158d194599723|2026-03-22 09:34:55 -0400|refactor(google): split credentials by domain concept
OAuth app management (OAuthAppCredentials, storeOAuthApp,
getOAuthApp, hasOAuthApp) moves to auth/oauth-app.ts.

Shared file I/O (readFile, writeFile, CredentialsFile)
moves to auth/store.ts so both modules can access the same
persistence file without duplication.

credentials.ts retains account and token management and
re-exports OAuth app symbols for backward compatibility.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
1d5241795c15ebea660acce7b048886611c826e5|2026-03-22 09:32:51 -0400|refactor(pr-review): split state into model and container
model.ts gets the domain types (PRTarget, CrawlResult,
ReviewSession, ReviewComment, etc.) and their manipulation
functions (addComment, updateComment, commentStats, etc.).

state.ts becomes a thin runtime container: PRReviewState,
createState, resetState, createSession. It re-exports
everything from model.ts so existing consumers keep working.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
23f044bca90e9764094dcfe16e48c62a68859fab|2026-03-22 09:31:19 -0400|refactor(pr-reply): clean up implementation.ts
shortSHAs was a one-liner wrapping commits.map(sha =>
sha.slice(0, 7)). Inlined at both callsites in handlers.ts
and replies.ts.

buildImplementationContext moves to briefing.ts where it
belongs alongside the other LLM briefing functions.

implementation.ts now focuses purely on git operations and
TDD coordination.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
9acc54addca54df8e8168e91ecea33a5e668759b|2026-03-22 09:27:28 -0400|refactor(commit-guardian): separate validation from rendering
renderValidation and renderCommitContent move from validate.ts
to review.ts where the rest of the UI lives. validate.ts
retains only the pure validation logic (CommitValidation type
and validate function) with no Theme or rendering dependency.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
42e040950e6e762af58a1e0279e37b117b8cecce|2026-03-22 09:25:35 -0400|refactor(google): dissolve params.ts utility module
The four parameter extraction helpers move to types.ts
alongside the ActionParams type they operate on. This
eliminates a standalone utility module that grouped
functions by mechanism rather than by concept.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
c6b844eeed0ed5406059c4f9fdae58fc401097da|2026-03-22 09:22:32 -0400|refactor(lib): move shared command parsing to library
The attribution interceptor imported from three guardians'
parse.ts files, creating cross-extension dependencies.

Git commit parsing (extractMessage, splitAtCommit,
extractCommitFlags, buildCommitHeredoc) moves to
lib/guardian/shell.ts alongside other shell utilities.

PR and issue command parsing (parsePrCommand, PrCommand,
parseIssueCommand, IssueCommand) moves to lib/github/cli.ts
alongside other gh CLI utilities.

Guardian parse.ts files become thin re-export shims plus
their rebuild helpers. The attribution interceptor now
imports exclusively from the shared library.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
fb5e4d6cbc5227843d0f1ffd008ec779bbcf68ce|2026-03-22 09:17:00 -0400|refactor(pr-reply): distribute api/repo.ts by domain
findPRForBranch and findDependentPRs move to api/github.ts
where they belong alongside other GitHub CLI queries
(getPRBranch, fetchReviews, postReply).

api/repo.ts retains resolvePR (orchestration) and
getCurrentBranch (local git query). Each file now has a
single cohesive responsibility.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
78355ad98a34a9501f407a844827473355ec2158|2026-03-22 00:49:29 -0400|refactor(pr-reply): dissolve navigation.ts
navigation.ts was a grab bag with four unrelated exports.
Each function moves to its domain home:

- resolvePR → api/repo.ts (PR discovery from branch)
- getPRBranch → api/github.ts (GitHub API query)
- readCodeContext + CodeContext → code-context.ts (own file)
- pushIfNeeded → implementation.ts (git push for commits)

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
5c402b1ed8578cdba3387e699986616dbfaee300|2026-03-22 00:47:14 -0400|refactor(pr-reply): extract dependency chain discovery
The three dependency chain functions (checkDependentPRs,
walkDependencyChain, fetchPRInfo) move from the 1073-line
handlers.ts to a focused dependency-chain.ts module.

handlers.ts drops to 973 lines and no longer mixes handler
orchestration with GitHub PR graph traversal.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
7686facabd8bfef09421be26d6dce8d29ace69e6|2026-03-22 00:44:32 -0400|refactor(pr-review): extract worktree and hunk clamping
Worktree management (create, remove, branch detection) moves
to its own worktree.ts module. Hunk range building and line
clamping move to lib/github/diff.ts where they belong as
general diff utilities, taking DiffFile[] rather than a
ReviewSession.

handlers.ts drops from 879 to 757 lines and no longer mixes
GitHub API concerns with git plumbing.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
aa284c36bed512af7c6351896b8942b2fda4eca1|2026-03-22 00:42:05 -0400|refactor(github): unify repo resolution
Both PR workflows duplicated the same "check current repo,
search disk, open new tab" logic with different result type
names. A single resolveRepo() in the shared repo-discovery
module replaces both, with a RepoResolution discriminated
union that uses clearer status names (opened-tab, open-failed
instead of switched, switch-failed, not-found-opened-tab-failed).

The pr-review api/repo.ts wrapper is deleted entirely. The
pr-reply api/repo.ts retains branch and PR discovery helpers
but loses its switchToRepo function.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
77d68fb795ddc7881407fbf6573be0fdbac2fa1a|2026-03-22 00:26:03 -0400|fix(lib): update lib/github/ internal imports after parse/ dissolve
Four files within lib/github/ still imported PRReference
and extractOwnerRepo from the deleted lib/parse/ path.
These were missed because the initial search only covered
consumers outside lib/github/.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
f94da27a6a0c2f6df133404378bd9d9e2ad8d4c6|2026-03-22 00:23:03 -0400|refactor(lib): mark PRReference and GhRebuildConfig as readonly
Both are value objects created once and never mutated.
Readonly fields make this contract explicit and prevent
accidental mutation.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
5d8c08e3884f3161eec51f81e14296a0d5815436|2026-03-22 00:22:07 -0400|refactor(pr-reply): move DependentPR to state.ts
DependentPR is a domain type used by handlers and API
modules, not a UI concept. It was defined in ui/panels.ts
because that's where the rebase confirmation panel lives,
but the type belongs with the rest of the domain model.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
682123c05fa1c888a1879db680c04d59341d8d29|2026-03-22 00:20:47 -0400|refactor(pr-review): remove DiffFile re-export from state
Consumers now import DiffFile directly from
lib/github/diff.ts instead of through a convenience
re-export in state.ts.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
f6d5d2fbe2a1c402592a91ec13565dbd957a3d48|2026-03-22 00:18:57 -0400|chore(ui): remove unused progress module
progress.ts exported ProgressTask, ProgressConfig and
progress() but nothing in the codebase imported them.
Dead code with no consumers.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
c3d86569c1f899be298d42e358e7dc6ffaa7704b|2026-03-22 00:17:22 -0400|refactor(pr-annotate): move ReviewComment and VetResult to types.ts
Domain types belong in a types file, not the registration
module. index.ts re-exports them for external consumers.
Internal modules (vet.ts, post.ts) import directly.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
6091a5ad08bba58b8cf359ba486591fda5cd39db|2026-03-22 00:16:20 -0400|refactor(pr-review): unexport CrawlConfig and CrawlProgress
Both types are only used within crawler.ts. No external
consumer needs them.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
5e0e7510a54ddf0630b6ebe0d15606f18e853a85|2026-03-22 00:14:32 -0400|refactor(ui): unexport clampVScroll, remove dead contentWidth
clampVScroll was exported but never imported outside
scroll-region.ts. Now a module-private function.

contentWidth was exported and never called anywhere,
including internally. The local variable with the same
name in renderScrollRegion masked it. Deleted as dead code.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
ea269ca8fd602d67fc26787a85a96b442dcf29d9|2026-03-22 00:06:09 -0400|refactor(ui): move scroll and height constants to owners
Scroll physics constants (FINE_SCROLL_LINES,
COARSE_SCROLL_OVERLAP_FRACTION, H_SCROLL_STEP,
HSCROLL_CONTENT_WIDTH, SCROLLBAR_GUTTER, MAX_CONTENT_WIDTH)
move from types.ts to scroll-region.ts. Height fraction
constants move to panel-height.ts.

types.ts is now the public API surface for UI types and
shared glyphs, not a dumping ground for internal tuning
values.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
ea5e4b092408f1d3e77cd9dfd630309ba0b18527|2026-03-21 23:50:11 -0400|refactor(pr-reply): remove deprecated navigation fields
The reviewIndex, reviewIntroduced and threadIndexInReview
fields were superseded by workspacePosition. No code read
or wrote them anymore. Removed from the interface, factory
function, persistence and restoration.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
828ea8f13b21722c77d6a41fab0557992de6754b|2026-03-21 23:48:32 -0400|refactor(lib): extract loadPlanDir and DEFAULT_PLAN_DIR
Both plan-workflow and pr-reply-workflow defined identical
copies of the plan directory loading logic. The constant
and function now live in lib/state.ts as the single source
of truth for plan directory configuration.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
2df63d0ad5ffd521a15f31c221dcb958dd417655|2026-03-21 23:43:35 -0400|refactor(ui): use Symbol.for() for panel height state
Replaces the string key on globalThis with a well-known
symbol. Prevents accidental collision with other global
state stored by extensions or pi internals.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
6e5b14d679662950e9becea40ea4ee795361874f|2026-03-21 23:39:44 -0400|refactor(ui): remove duplicate CONTENT_INDENT definition
text.ts redefined the same constant already exported from
types.ts. Now it imports and re-exports the single source
of truth.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
55103140ee6ad95f1df734b4979fb1e2b9b968cd|2026-03-21 23:38:15 -0400|refactor(lib): delete re-export shims for pr-reference
Consumers now import PRReference and parsePRReference
directly from lib/github/pr-reference.ts instead of
going through single-line re-export files in each PR
workflow's api/ directory.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
6b44546dae64c9f7d6ca6c70a264bb3936817855|2026-03-21 23:35:35 -0400|refactor(lib): dissolve lib/parse/ directory
Shell parsing utilities now live in lib/guardian/shell.ts,
alongside the guardian pipeline that consumes them. GitHub
CLI modelling moves to lib/github/cli.ts, and PR reference
parsing moves to lib/github/pr-reference.ts.

The parse/ directory grouped files by technical layer
("parsing") rather than by domain. After this move, lib/
screams its domain concepts: guardians, GitHub, UI.

AGENTS.md updated to reflect the new structure.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
9a15566fff01a53d445ba4353f63f09399700489|2026-03-21 23:22:22 -0400|refactor(pr-reply): group showSummaryPanel parameters
Replace 8 positional parameters with a SummaryPanelConfig
object. The callsite in handlers.ts now reads as a named
config rather than an opaque argument list.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
d23f2fa2a7a6d5e906376c629b31a7e7f0fc4efa|2026-03-21 23:21:27 -0400|fix(pr): validate LLM input at tool boundaries
Add type guards for recommendation and verdict strings
received from the LLM. Both were previously bare as-casts
on unvalidated input.

pr-reply: isRecommendation validates thread analysis
recommendations, falling back to "pass" for invalid values.
pr-review: isReviewVerdict validates the verdict string,
silently ignoring invalid values so the existing default
remains.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
beaf924b5a28c6aee9f767731186a46019ebba3a|2026-03-21 23:19:08 -0400|refactor(ui): split formatRedirect into reason and block
Replace the single formatRedirect function with two:
formatRedirectReason (returns the string) and
formatRedirectBlock (returns { block, reason }).

Guardians call formatRedirectBlock since they return
guardian results directly. Google workspace confirmation
calls formatRedirectReason since it only needs the string.
Each consumer gets the shape it needs without unwrapping.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
d0b58b0e555902b3c327f3e722c4fa6841b719a4|2026-03-21 23:15:25 -0400|refactor(guardian): remove event from review() signature
The event parameter was unused in all four guardians — each
received _event and never accessed it. Remove it from the
CommandGuardian interface and the registerGuardian callsite.

Guardians operate on parsed data, not raw events. If a
future guardian needs the raw command, it can derive it
from the parsed representation.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
82ef075878f178465d2c2c017289d1e4753eaeea|2026-03-21 23:11:53 -0400|refactor(guardian): add ALLOW constant for GuardianResult
Export ALLOW from lib/guardian/types.ts as a named constant
for the "allow command as-is" return value. Replace bare
return undefined in all four guardians' approve branches
with return ALLOW so the intent is self-documenting.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
77bd81aaa99b86cb5ed31fbccd3e628b1aff2d23|2026-03-21 23:05:45 -0400|refactor(ui): discriminate PromptResult by source
Split the overloaded "action" variant of PromptResult into
three discriminated variants: "action" (key from action
bar), "option" (value from option list) and "redirect"
(feedback note). Actions carry a key field, options carry
a value field with optional editorText.

Apply the same key-over-value change to WorkspaceResult
for consistency.

Update all 22 consumer files to match on the correct
variant and field. Option-based consumers (ask-workflow,
plan-workflow interview, auth-flow) now check
type === "option"; action-based consumers check
type === "action" with the key field.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
62645bf508c7dd963a5c6d9f8bb28b7d9471aaff|2026-03-21 22:56:34 -0400|fix(tdd): use promptTabbed for refactor gate
The refactor gate passes items and canAddItems, which is a
tabbed config. The previous commit incorrectly migrated it
to promptSingle, where it would fail because
SinglePromptConfig has no items field.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
000b680ec9591f1bcb4e71bec63645f718d3ec95|2026-03-21 22:53:33 -0400|refactor(ui): replace prompt() overloads with named functions
Export promptSingle() and promptTabbed() from lib/ui/panel.ts,
each returning the exact type without narrowing. Keep prompt()
as a deprecated wrapper for backward compatibility.

Migrate all 16 consumers to the explicit functions. This
eliminates the overload ambiguity that caused a double cast
in plan-workflow/interview.ts and fixes a real bug where the
empty-questions path always returned null because TabbedResult
has no type field and the result.type !== "object" guard
was unreachable.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
7419a748c2a4bace7aacb22b7acae0429ea50ff5|2026-03-21 22:46:29 -0400|feat(ui): wire title into prompt configs
SinglePromptConfig and TabbedPromptConfig now accept an
optional title field. Both prompt variants render it below
the top border in accent+bold, matching the existing
view() title treatment. Chrome height calculations account
for the two extra lines (title + blank) so scroll budgets
remain correct.

Four consumers already pass title but it was silently
dropped because the types had no such field. Those
callsites now work as intended: commit-guardian,
pr-guardian, issue-guardian and tdd-workflow transitions
all display their titles.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
73bfe8961bbb35b0bbb38fab505451c6a9b1a668|2026-03-21 21:37:55 -0400|docs: rewrite AGENTS.md and fix all cross-references
Rewrites AGENTS.md to use the new taxonomy: extension
categories with contract suffixes, skill categories with
type suffixes, and updated references throughout.

Also fixes all remaining stale cross-references in skill
README.md files, extension README.md files, and code
comments that still referenced old names.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
d3a1b91d6854d85ff930867bb1bc60868318c1dc|2026-03-21 21:32:31 -0400|refactor(extensions): update runtime keys and references
Updates all internal string keys and comments to match the
new extension names. Runtime state keys change from
plan-mode/tdd-mode to plan-workflow/tdd-workflow. All
comments referencing old extension directory names in lib
modules and READMEs are updated for consistency.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
96dec87190364ee675d65f0639946cd13a7fa519|2026-03-21 21:19:05 -0400|refactor(extensions): rename all extensions to new taxonomy
Applies the naming taxonomy with contract suffixes for every
extension. Each suffix identifies the behavioural contract:

- guardian: command interception with human review gate
- interceptor: silent command modification
- workflow: multi-step or session-wide process with state
- integration: bridge to external service
- widget: UI elements

Renames:
- ask → ask-workflow
- attribution → attribution-interceptor
- content-viewer → content-viewer-widget
- google-workspace → google-workspace-integration
- panel-height → panel-zoom-widget
- plan-mode → plan-workflow
- pr-annotate → pr-annotate-workflow
- pr-reply → pr-reply-workflow
- pr-review → pr-review-workflow
- status-line → status-line-widget
- tdd-mode → tdd-workflow
- web-search → web-search-integration

Also removes test-harness extension and updates all JSDoc
headers. Runtime state keys (plan-mode, tdd-mode) are left
as-is to preserve session state compatibility.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
cfe0c95849ddfc8c1b79f66b1ef69296e742f205|2026-03-21 21:12:04 -0400|refactor(skills): rename all package skills to new taxonomy
Applies the naming taxonomy from the taxonomy-guide skill.
Every skill now follows {domain}-{concern}-{suffix} where
the domain groups by subject matter and the suffix describes
what the skill is.

Domains: code, git, github, google, planning, prose, and
no prefix for technology-neutral formats.

Suffixes: -guide (instructional), -convention (tool rules),
-format (artifact templates), -standard (quality preferences).

Notable changes beyond simple renames:
- git-commit-format + git-hygiene merge into
  git-commit-convention (operational conventions)
- Conventional commits spec splits into commit-format
  (technology-neutral)
- conventional-comments becomes comment-format
  (technology-neutral)
- All inter-skill cross-references updated
- README.md titles updated to match new names

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
036970708f810c5a4478887f399114d2d8d5cab6|2026-03-21 21:07:20 -0400|refactor(skills): move extension skills to project-local
The extension-development and extension-keybindings skills
are only useful when developing this package, not to
consumers who install it. They belong in .pi/skills/ as
project-local skills rather than in ./skills/ as
package-bound skills.

Renames to match the new taxonomy:
- extension-development → extension-dev-guide
- extension-keybindings → extension-keybinding-guide

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
35f15564636b142aba8812a3f251e96c1a11d652|2026-03-21 21:04:23 -0400|docs(taxonomy): add taxonomy skill and track .pi/skills/
Establishes the naming taxonomy for skills and extensions
in a project-local skill at .pi/skills/taxonomy-guide/.
Covers domain prefixes, type suffixes, location rules and
the decision framework for when to create a new skill vs
extension vs adding to AGENTS.md.

Updates .gitignore to track .pi/skills/ while keeping the
rest of .pi/ (plans, settings) ignored.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
a4ff30ec8558b67e0876a24d2e53431903b8a035|2026-03-21 18:24:40 -0400|Redesign Extension Keybindings for Consistency (#57)
* refactor(ui): rename steer to redirect

Rename the internal "steer" concept to "redirect" across
all extensions. The trigger changes from Shift+Enter to
Shift+Escape, and the render hint updates accordingly.

Type renames: steer → redirect, steerAction →
annotatedAction, pureSteer → redirect. Config renames:
steerHint → redirectHint. File rename: steer.ts →
redirect.ts. All consumer code (guardians, modes, tools)
updated to match.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

* refactor(ui): change view keys from letters to numbers

All view switching keys within tabs now use sequential
numbers (1, 2, 3) instead of letters. This makes views
structurally impossible to collide with action keys.

Affected extensions: pr-annotate, pr-review (overview,
review and submit panels), pr-reply, plan-mode interview
and ask tool.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

* refactor(ui): converge verb set across extensions

Unify action labels and keys to a consistent verb set:
Approve (a), Reject (r), Pass (p), New (n), Delete (d).

History guardian: Allow/Block → Approve/Reject. PR review
and annotate: Handled → Pass, + → n for New, remove
standalone Steer action from comment views. Plan
interview: Skip → Pass. PR reply: remove Defer and
Implement Later, merge Skip into Pass, Enter opens
threads.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

* refactor: remove global keyboard shortcuts

Remove Ctrl+Alt shortcuts for plan mode (p), TDD mode (t),
PR review (v) and PR reply (r). Each has an equivalent
slash command. Panel height toggles (Ctrl+Alt+F/M) are
kept as the only global shortcuts since they must work
during panel display.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

* refactor(ui): unify footer rendering

Replace separate renderActionBar + buildHintBar calls with
a single renderFooter function. The footer renders a
two-row grid when actions are present (actions + modifiers
on row 1, navigation + decisions on row 2) or a single
row when there are no actions.

Action rendering and view hint formatting move from
action-bar.ts to panel-layout.ts. The action-bar module
retains only input handling (handleActionInput).

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

* docs: create extension keybindings skill

Add skills/extension-keybindings/ with SKILL.md and
README.md. The skill codifies the five rules, universal
input model, verb set, view numbering, footer grid layout
and conventions for adding new gates and workspaces.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

* docs: update keybinding audit for overhaul

Reflect all changes from the keybinding overhaul in the
audit document. Updated sections: global shortcuts (now
only panel height), guardian verbs, plan mode views, PR
annotate/review/reply keys, ask tool view key and
available key space analysis.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---------

Co-authored-by: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>
---COMMIT_END---
f19b376d6ab85f456ffc2aa84003b23fd6842119|2026-03-21 15:49:56 -0400|Improve Scroll Granularity and Add View Key Toggle (#56)
* feat(ui): improve scroll and view switching

View keys now toggle: pressing the active view's key
returns to the first view (Overview). This makes it easy
to glance at Comments or Source and then jump back without
reaching for a different key.

Vertical scrolling is split into two speeds. Shift+↑↓ is
a fine scroll (3 lines), while PageUp/Down and Ctrl+U/D
do a half-page coarse scroll that keeps 50% of the
previous view visible. The old behaviour scrolled nearly
a full page, which made it hard to maintain context.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

* fix(ui): drop Ctrl+U/D scroll bindings

Pi claims Ctrl+U (deleteToLineStart) and Ctrl+D (app.exit)
globally, so those keys never reach our panel input handlers.
PageUp/Down is the coarse scroll binding now.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

* fix(ui): use Mac-friendly scroll hint

Fn+↑↓ is how Mac keyboards produce PageUp/Down. The hint
bar now says that instead of "PgUp/Dn" which doesn't
correspond to any visible key on the keyboard.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

* fix(ui): remove stale Ctrl+U/D mention

The doc comment on COARSE_SCROLL_OVERLAP_FRACTION still
referenced Ctrl+U/D from before those bindings were dropped.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---------

Co-authored-by: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>
---COMMIT_END---
c8d89fe68b5ef8dd6285ada081840b95bbbcd88a|2026-03-21 02:15:12 -0400|feat(skills): add code-style skill (#55)
Define personal code design and style preferences as a
portable skill that travels across projects. Covers
readability, cognitive load, naming, structure, abstraction,
testing, domain-driven design, consumer-driven design and
the principle of least surprise.

Code-specific comment guidance (inline comment formatting,
section-divider prohibition, doc comment warmth) moves from
writing-voice into code-style so each skill owns its domain
cleanly.

Co-authored-by: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>
---COMMIT_END---
ce3a3aef762ae4d6d69d14fed5050f56c0d5b9d6|2026-03-21 00:16:16 -0400|fix: typo in calendar-handlers comment
The batch replacement left a stray period in "event. to"
which should have been "event to".

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
4a0d4e7506c27e46fc38997e633a55b9c82727b1|2026-03-21 00:10:58 -0400|style: make code comments complete sentences
Rewrite fragment comments across the codebase so every
comment that expresses a thought (describes an action or
explains reasoning) has a subject and a verb. Short labels
naming what follows (// Approve, // Headers, etc.) are left
alone per the noun-phrase label rule.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
80d20d2722212a9d7a208fa9f2a1920d5194fb29|2026-03-20 23:56:33 -0400|style: remove category divider comments
Remove all section divider comments (// ---- Foo ----,
// ── Foo ──, // Helper functions, etc.) across the
codebase. The code's structure should make organization
obvious without signposts.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
abd8d08dd857f34fe15c66d1ceaee1aee1bd869f|2026-03-20 23:54:17 -0400|docs(writing-voice): add code comment rules
Add a Code Comments subsection under Audience that spells
out three rules: "why" comments must be complete sentences
with a subject and verb, short functional markers are fine
as labels, and category/section divider comments are a code
smell that shouldn't be written.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
439b440d714fa07a9c91c9eea939813177e0d80b|2026-03-20 23:49:24 -0400|style: warm up inline code comments
Rewrite inline comments that explain "why" for a more
conversational tone. Functional markers and section headers
are left alone; only the explanatory comments get warmed up.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
446902c0cffc20b520a973da1575f94d4045afcf|2026-03-20 23:46:25 -0400|style: warm up JSDoc file-level headers
Rewrite terse metadata-style JSDoc headers into warm,
explanatory descriptions. Focus on file-level headers in
mode extensions, the shared library, and domain modules
where the original read like inventory labels rather than
explanations of what the module does and why.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
3d8db9d3d907cd0fedcb0e53e960964514f3d2c1|2026-03-20 23:41:08 -0400|style: rewrite READMEs for conversational voice
Apply full human-audience conversational treatment across
all README.md files. Use contractions, warm phrasing, and
a tone that sounds like explaining something to a colleague
rather than writing a spec.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
f46caae2c889606afe023716b76dbd72b1a662cb|2026-03-20 23:34:43 -0400|style: warm up AGENTS.md explanatory prose
Rewrite explanatory paragraphs in the design principles and
conventions sections for a warmer, more conversational tone.
Rules and directives stay directive; only the context and
rationale sections get softened.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
b607d7f3565242c52d12208929b9ba50902d5b2e|2026-03-20 23:32:31 -0400|style(skills): warm up explanatory prose
Rewrite explanatory paragraphs across skill files for a
warmer, more conversational tone. Commands and structure
stay intact; only the "why" and "context" sections get
the treatment.

Affected skills: pr-writing, tdd-workflow, pr-review,
pr-reply, code-review-criteria, conventional-comments,
git-rebase-resolution, extension-development, plan-workflow,
issue-writing and dev-planning.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
bec442be4f631be34fbe43cf671faf590ca977c6|2026-03-20 23:27:15 -0400|style(skills): strip periods from noun-phrase list items
The first writing-voice pass added terminal periods to all
list items uniformly. Noun-phrase items (things to look for,
check or consider) shouldn't have them; only imperative items
(instructions with a verb) get periods.

Affected files: code-investigation, code-review-criteria,
tdd-workflow and pr-writing.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
9656e688465b50825a8861bdafc2b1a9c9bc3cee|2026-03-20 23:24:46 -0400|docs(writing-voice): add audience strategies
Add an Audience section encoding the human-audience and
LLM-audience writing strategies. Human-audience content
(readmes, doc comments, inline comments) gets full
conversational treatment. LLM-audience content (agent skills,
project guidelines, instruction strings) keeps commands
directive while warming up explanatory prose.

Also update the Lists section with the noun-phrase vs
imperative distinction and expand the frontmatter description
to cover agent-facing content.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
65d7ddb3e149fedeb035b8e5bfa893123fa7e798|2026-03-20 22:57:36 -0400|style: apply writing voice across the repo
Replace emdashes with colons, semi-colons and
parentheses throughout all markdown and TypeScript
files. Switch American English spellings to Canadian
(behaviour, colour, colouring, coloured, judgement,
neighbour, licence). Remove Oxford commas where
ambiguity isn't a concern. Title Case all markdown
headings in AGENTS.md. Ensure list items in concept
lists are complete sentences with terminal punctuation.

Covers 149 files: AGENTS.md, README.md, all extension
READMEs, all skill SKILL.md and README.md files, and
prose in JSDoc comments, inline comments and string
literals across the TypeScript codebase.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
b709f5b96a68805502e5eb1c73f5e47daf79adc1|2026-03-20 22:43:47 -0400|feat(skills): add writing voice skill (#53)
Captures personal writing style preferences so that any
prose the agent produces feels authentic and consistent.
Covers Canadian English spelling, punctuation rules (no
emdashes, Oxford comma only when needed for clarity),
conversational tone with contractions, complete sentences,
list formatting, Title Case headings, commit message body
guidance, and kind language when addressing people.

Co-authored-by: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>
---COMMIT_END---
0f7702e550c746a449b7cd0850763892a80f898e|2026-03-20 22:12:48 -0400|fix(ui): handle + key in action bars (#52)
matchesKey(data, "+") always returns false because pi-tui's
parseKeyId splits on + as the modifier separator, yielding
an empty key. This made the New comment action silently
fail in both pr-review and pr-annotate comment views.

Add matchesActionKey helper that falls back to
Key.shift(baseKey) for Kitty protocol terminals and raw
character comparison for legacy terminals.

Co-authored-by: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>
---COMMIT_END---
7c70c4249322e554e25d5e936d5855fbf045f130|2026-03-20 20:37:42 -0400|refactor: type tool_call handler return values (#51)
Use the new ToolCallEventResult export from pi 0.61.1 to
annotate all three tool_call event handlers. This makes the
handler contract explicit without changing runtime behavior.

Also updates enforcePlanMode to return ToolCallEventResult
instead of an inline type literal, keeping the types aligned
across the codebase.

Co-authored-by: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>
---COMMIT_END---
963f8665f528763cf8f1ba1c69d41ec641ba6614|2026-03-20 20:15:01 -0400|fix(pr-review,pr-reply): pass user request to (#50)
cross-repo tab handoffs

When activating pr_review or pr_reply for a PR in a
different repo, the extension opens a new terminal tab.
Previously the new tab received a bare prompt like
"review owner/repo#123" with no context about what the
user actually asked for, and the original tab's agent
kept retrying activate in a loop.

Now the agent passes the user's original request via a
user_request parameter. The extension uses it as the new
tab's prompt so the agent there can replicate the user's
intent. The original tab gets a clear "this task is
complete" message that stops the retry loop.

Co-authored-by: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>
---COMMIT_END---
d99ec5a15676bb536a6e5a0dcdae362f4cd30ca2|2026-03-20 14:43:15 -0400|fix(skills): make PR and issue assignment mandatory
The pr-writing and issue-writing skills didn't explicitly
require assigning yourself after creation. The gh-cli
skill mentioned it as a formatting preference but not as
a mandatory step. This led to PRs being created without
assignees.

Add explicit "After Creating" sections to both skills
with the exact command to run immediately after creation.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
30e9bb90199887e855c2bff578231f82fe8f8bd8|2026-03-20 14:35:02 -0400|fix(attribution): use italic instead of sub for footer
The <sub> tag renders too small on GitHub. Italic text is
subtle enough to not compete with the PR body while still
being readable.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
e0eee06d136817a24b731d651a18f906fb665085|2026-03-20 14:32:06 -0400|fix(attribution): fix PR/issue footer rendering
Add blank line before the --- separator to prevent GitHub
from treating the preceding paragraph as a setext h2 heading.
Wrap the attribution text in <sub> tags so it renders as
small muted text instead of competing with the PR body.

Co-Authored-By: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>

---COMMIT_END---
e2b1a7d333519329d02f4d468c58b2e0858273a6|2026-03-20 14:29:36 -0400|feat(attribution): add AI co-authorship extension (#49)
Injects Co-Authored-By attribution into commits, PRs, and
issues created through Pi. Makes AI involvement transparent
for analytics tooling like Shopify's AI PR dashboard.

Commits get a git trailer with the active model name. PRs and
issues get a markdown footer. The extension loads before
guardians alphabetically, so users see the attribution during
review.

On by default. Disable with --no-attribution flag.

Co-authored-by: AI (Claude Opus 4.6 via Pi) <noreply@pi.dev>
---COMMIT_END---
94fca7fa322a4b8f6786c8667f09808ced35ad84|2026-03-18 18:04:12 -0400|Converge PR Extension UX with Shared Infrastructure (#48)
* refactor(lib): extract shared GraphQL runner

Both pr-review and pr-reply had identical runGraphQL
helpers that execute typed GraphQL queries via gh CLI.
Consolidate into lib/github/graphql.ts so both extensions
import from one place.

* refactor(lib): extract shared repo discovery

Move repository discovery, terminal detection, and tab
spawning from pr-review and pr-reply into a shared module
at lib/github/repo-discovery.ts. Both extensions had nearly
identical implementations of findRepoOnDisk, isGitRepo,
detectTerminal, spawnWezterm, and spawnGhostty.

Also consolidates getCurrentRepo and getRepoRoot into the
shared module. Extension-specific wrappers (resolveRepo,
switchToRepo) remain in their extensions.

* refactor(lib): extract shared diff fetching and parsing

Move fetchDiff, parseDiff, and the diff types (DiffFile,
DiffHunk, DiffLine) from pr-review into lib/github/diff.ts.
These were tightly coupled to pr-review's state.ts but are
pure data structures needed by pr-annotate for its upcoming
workspace with diff context.

pr-review's state.ts re-exports the types for backward
compatibility. api/github.ts re-exports the functions.

* refactor(lib): extract shared review posting

Move the temp-file review posting pattern into
lib/github/review-post.ts. Both pr-annotate and pr-review
wrote JSON payloads to temp files and called gh api — now
they share one implementation.

pr-annotate's post.ts now resolves owner/repo from git
remote when not explicitly provided, instead of relying
on gh CLI template expansion.

* feat(pr-annotate): upgrade to workspace-style panel

Replace the one-tab-per-comment vetting flow with a
workspace panel matching pr-review's interaction pattern:

- Summary tab with overall progress and file breakdown
- Per-file tabs with three views each:
  - Overview: diff with comment indicators on annotated lines
  - Comments: selectable list with approve/reject per comment
  - Source: full file, syntax highlighted
- Tab auto-completion when all comments are resolved
- Ctrl+Enter to submit when all tabs complete
- Steer (Shift+S) and new comment (+) flow through the
  workspace editor

The diff is fetched via the shared lib/github/diff module
during tool execution, giving workspace users real diff
context around their self-review comments.

Steer results now preserve already-approved comments so
they aren't lost in multi-round flows.

* fix(ui): cap renderMarkdown width for hscroll panels

When horizontal scrolling is enabled, content functions
receive width=10,000 so code and diffs can extend beyond
the viewport. But renderMarkdown passes that width to Pi's
Markdown component, causing prose to render without wrapping.

Cap renderMarkdown to terminal columns internally. Prose
always wraps to screen width, code and diffs still extend
for horizontal scrolling. Mixed views just work.

wordWrap already handled this via contentWrapWidth(). Now
both prose-rendering mechanisms behave consistently.

* feat(pr-reply): add workspace and batch analysis

Redesign pr-reply from sequential thread gates to a
workspace-based flow matching pr-review's patterns:

- New `generate-analysis` action for batch pre-analysis
  of all threads at once (parallel to pr-review's
  generate-comments)
- New reviewer-tab workspace with three views per tab:
  Overview (reviewer summary), Threads (selectable list
  with conversation + recommendation), Source (code context)
- Summary tab with PR overview and progress tracker
- Thread actions (implement/reply/defer/skip) inline in
  the workspace
- Defer and skip handled inline without dismissing
- Implement and reply dismiss the workspace, returning
  the action for the LLM to handle
- Workspace position preserved across dismiss/restore
  cycles for implementation

The `review` action now shows the workspace (replaces the
old single-prompt review overview). Legacy `next`/`show`
actions kept for backward compatibility.

State model extended with ThreadAnalysis, ReviewerAnalysis,
WorkspacePosition, and currentThreadId.

* refactor(pr-review): rename raW view to Source

Change the third view key from 'w' to 's' and label from
'raW' to 'Source' across all pr-review file tabs. This
aligns with the convention used by pr-annotate and pr-reply:

  [o] Overview  [c] Comments  [s] Source

The 's' key is context-sensitive: in Comments view, the
view's handleInput intercepts it for Steer (step 1 in
input priority). In Overview view, it switches to Source
(step 4). The action bar shows the correct hint per view.

* docs: update pr-reply skill for workspace flow

Rewrite the pr-reply SKILL.md to reflect the new workspace-
based workflow: activate → generate-analysis → review →
(implement|reply) → review → deactivate.

Removes documentation of the legacy next/show/review loop
in favor of the batch analysis + workspace pattern.

* feat(pr-reply): re-analyze threads after state changes

After implementation (done) or reply posting, the handler
now instructs the LLM to re-analyze all remaining pending
threads with fresh code context before reopening the
workspace. This prevents stale recommendations when
implementation of one thread changes code that other
threads comment on.

The flow becomes: activate → generate-analysis → review →
(implement|reply) → done → generate-analysis → review → ...

Each generate-analysis call reads current code state, so
recommendations are always fresh when the workspace opens.

* docs: update skills and READMEs for converged UX

- pr-review SKILL.md: raW (w) → Source (s)
- pr-annotate README: describe workspace with file tabs
- pr-reply README: describe workspace flow with batch
  analysis and re-analysis loop

* fix(pr-reply): workspace browses, gate decides

The workspace was showing bare thread comments with no code
context — unusable for making decisions. Redesigned to split
responsibilities:

- Workspace is the NAVIGATION surface: browse reviewer tabs,
  see thread status and recommendations, skip/defer inline
- Enter on a thread opens the existing FULL-CONTEXT GATE
  with code, conversation, LLM recommendation, and actions
- Gate returns the decision, handler routes to implement/reply

Also fixes force-redraw on invalidate to prevent differential
rendering artifacts (compounding "Working..." on navigation).

* fix(pr-reply): workspace UX improvements

- Remove global "Enter thread" action — only shows on the
  Threads view where it's actionable
- Threads view is now the default (first) view when
  switching to a reviewer tab, so ↑↓ navigation works
  immediately
- Include thread IDs in batch analysis briefing so the LLM
  can reference them in generate-analysis responses, fixing
  empty recommendations in the thread gate

* fix(pr-reply): make reply steer feedback more explicit

Replace the generic formatSteer wrapper with direct text
that quotes the user's exact feedback, includes the original
draft, and explicitly tells the LLM to rewrite incorporating
the feedback. The previous format buried the user's words
in a generic template.

* refactor(pr-reply): merge overview and threads into one view

Each reviewer tab now has a single view: reviewer header
and body at top, navigable thread list below. No separate
Overview/Threads split — they weren't providing value apart.
---COMMIT_END---
39ab620a4788512dc644ce960a071912b7346b66|2026-03-17 20:47:42 -0400|docs(pr-annotate): add line range selection guidance (#46)
The agent frequently selects line ranges that have little or
nothing to do with the comment content, making annotations
confusing for reviewers.

Add a "Choosing Line Ranges" section to the pr-writing skill
that teaches range scoping: match the range to the code the
comment discusses, scope by construct type, and avoid over-
or under-selecting. Split the old "Getting Line Numbers Right"
into range selection (new) and diff validation (existing).

Update pr_annotate prompt guidelines to reinforce range/comment
coherence at call time — the range must contain the specific
code the comment body discusses, scoped tightly to the relevant
construct.
---COMMIT_END---
076f872c3d6c3d42e771c81f5bfa869db981d4da|2026-03-17 20:43:06 -0400|fix(ui): enable horizontal scrolling in panels (#45)
Horizontal scrolling never worked because content renderers
truncated lines to the viewport width before the overflow
check ran. maxContentWidth could never exceed the viewport,
so cachedHScroll was always false.

When allowHScroll is enabled, content functions now receive
HSCROLL_CONTENT_WIDTH (10,000) instead of the viewport width.
Lines render at their full length, the scroll region detects
overflow, and horizontalSlice handles the viewport.

Also clamps the horizontal offset so users cannot scroll
past the end of the content. handleScrollInput accepts a
maxHOffset parameter computed from the content width minus
the viewport width.

Fixes all four panel types: view, prompt-single,
prompt-tabbed, and prompt-workspace.
---COMMIT_END---
be614d21aa1411824fde6d924615106b099dab19|2026-03-17 20:29:30 -0400|Add Panel Height Modes with Keyboard Shortcuts (#44)
* feat(ui): add panel height modes

Replace the single MAX_HEIGHT_FRACTION constant and remove
PI_CHROME_LINES with three panel height modes: minimized
(0.25), normal (0.6), and fullscreen (0.9).

PI_CHROME_LINES was an imprecise estimate of pi's own UI
overhead subtracted from the content budget. Pi doesn't
expose its chrome height, and the constant just reduced
the usable panel space without accurately preventing
overlap. Removing it lets MAX_HEIGHT_FRACTION directly
control the panel's visual footprint.

State lives on globalThis so it's shared across pi's
per-extension module boundaries.

* feat(panel-height): add toggle shortcuts

Register Ctrl+Alt+F to toggle fullscreen and Ctrl+Alt+M to
toggle minimized. Both return to normal when toggled again.

Uses onTerminalInput instead of registerShortcut so the
shortcuts work even while a panel component has focus.
Filters key release events to prevent the Kitty keyboard
protocol from double-toggling on press and release.

* feat(status-line): show panel height mode indicator

Display a geometric glyph between extension statuses and
context info on the right side of the status bar. Uses dim
styling to match the surrounding segments.

Glyphs: ━ minimized, □ normal, ■ fullscreen.
---COMMIT_END---
462e15fdddb6c2ef8b4f945844056c8d6fabe22b|2026-03-17 15:40:18 -0400|Rewrite PR Review Extension With Deep Context Crawling and AI-Generated Comments (#43)
* feat(pr-review): M0 — tear down and establish new skeleton

Remove existing handler/UI/briefing code and replace with
the new module skeleton for the ground-up redesign.

Deleted: worktree.ts, all old UI panels (context-summary,
description, file-review, vetting).

Gutted: handlers.ts (stub handlers for 10 new actions),
briefing.ts (empty), state.ts (new domain model with
CrawlResult, ReviewComment with categories, TabState,
ReviewSession with phases), transitions.ts (adapted to
new state).

Updated: index.ts (new ACTIONS enum, new tool parameters
including generate-comments with synopsis/scope_analysis/
source_roles/comments), api/types.ts (added reviewer and
deep issue GraphQL types), api/github.ts (added reviewer
parsing, removed previous review fetching).

Created stubs: crawler.ts, api/repo.ts, and four new UI
panels (overview, review, submit, comment-detail).

* feat(pr-review): M1 — deep context crawler

Build crawler.ts with recursive link crawling up to 5 levels
deep. The crawler follows GitHub references (#123, URLs,
cross-repo links) with loop detection via a visited set.

Level 0 fetches PR metadata, diff, comments, and reviewers.
Level 1 crawls linked issues deeply (parent/sub-issues via
trackedInIssues/trackedIssues, cross-references from timeline).
Levels 2-4 follow references found at previous levels. Level 5
stops and sets hitDepthLimit.

Source file discovery uses rg to find imports from changed
files, reverse imports (files that import changed files), and
corresponding test files.

Added ISSUE_DEEP_QUERY for recursive issue fetching with
parent/sub-issue relationships and timeline cross-references.

* feat(pr-review): M3 — gathering phase with repo resolution

Build api/repo.ts for repository discovery and terminal tab
switching. Implement handleActivate with the full crawl flow:
parse PR reference, resolve repo on disk, crawl deep context,
store session, and return comprehensive briefing.

Implement handleGenerateComments to receive agent analysis
(synopsis, scope analysis, source file roles) and structured
comments. Also implement handleAddComment, handleUpdateComment,
handleRemoveComment, handlePost, and handleDeactivate with
real logic.

The activation briefing includes full diff, PR description,
issue summaries, reviewer status, reference summary, source
files, and instructions for the agent to generate comments.

* feat(lib/ui): M6 — workspace prompt primitive

Add a new shared prompt variant for stateful tabbed
interaction. Unlike prompt-tabbed (where each tab produces
one result), workspace tabs are persistent workspaces where
the user acts many times with in-place state mutation.

Key capabilities:
- Per-view input handlers that get first crack at input
  (enables selectable lists, custom key handling)
- External tab status via callback (business logic drives
  completion, not result collection)
- Content invalidation for re-rendering after state changes
- View-specific actions alongside global actions
- Steer via inline note editor

New files: lib/ui/prompt-workspace.ts (implementation),
workspace types in lib/ui/types.ts, workspace() overload
in lib/ui/panel.ts.

* feat(pr-review): M5 — overview panel with three tabs

Build the Phase 1 overview panel using the workspace prompt
primitive. Three tabs:

Overview — PR metadata, reviewers with verdict colors, AI
synopsis, and depth limit warning banner.

References — selectable list of all discovered references
grouped by type (issue/PR/commit/file/external) with icons,
source attribution, and depth indicators. Enter opens URLs
in the system browser.

Source — selectable list of source files with role
descriptions shown when selected. Enter opens GitHub URLs.

Wire up handleOverview to show the panel and handle review
transition, steer feedback, and escape.

* feat(pr-review): M7/M8/M9 — review panel with three modes

Build the Phase 2 review panel using the workspace prompt.
Tabs: Desc, Scope, and one per changed file. Each tab has
three views:

Overview (o) — diff with comment indicators for file tabs,
rendered markdown for desc/scope tabs.

Comments (c) — selectable comment list with status glyphs
(●/◆/✕), expanded discussion for selected comment, and
actions: approve, reject, steer, and new comment.

Raw (w) — full file content syntax-highlighted for file
tabs, raw markdown for desc, file stats for scope.

Comment interaction: approve/reject mutate state in-place
with content invalidation. Tab auto-completes when all
comments are resolved. Manual 'h' key for explicit handled
marking. Selection advances to next pending after action.

Wire up handleReview with persist after panel closes.

* feat(pr-review): M11 — submit panel and comment detail

Build the submit panel showing review body, verdict with
color coding, comment summary by category (title/scope/file),
and approved comment one-liners. Post action submits to
GitHub. Steer lets the user edit body/verdict via LLM.

Build the comment detail view — a read-only display with
file path, line range, label, decorations, subject, and
full discussion text.

Wire up handleSubmit with body/verdict update support
and direct post-from-panel flow.

* docs(pr-review): M13 — rewrite skill file for new workflow

Replace the skill instructions with the new two-phase
workflow: activate → generate-comments → overview → review
→ submit → post → deactivate.

Cover comment generation with category rules, overview/review
phase interaction, steer handling patterns, complexity
adaptation for small vs large PRs, and updated action table.

* feat(pr-review): M14 — lifecycle with transparent re-crawl

Add ensureContext helper that re-crawls PR context when the
session was restored from persistence but context was lost.
Overview and review handlers now transparently re-fetch
instead of failing when context is null.

Persistence, restore, context injection, status line, and
detail widget were already implemented in earlier milestones.

* feat(pr-review): M15 — polish renderResult and README

Update renderResult to show meaningful one-liners for all
actions: activate shows file/reference/reviewer counts,
generate-comments shows comment count, overview/review/submit
show panel name with steer indicator, post shows verdict and
comment count.

Rewrite README.md to describe the new two-phase architecture,
deep context crawling, workspace prompt primitive, and
comment lifecycle.

* fix(pr-review): remove PR number from status bar

The status bar and detail widget were both showing the PR
number, causing visual stutter. Follow the TDD mode pattern:
status bar shows just "PR Review", detail widget carries the
PR-specific context.

* fix(pr-review): limit source files to diff files only

Reverse import and test file discovery used basename-based
rg searches that produced too many false positives in large
repos. A 3-file PR touching table.rb was showing 47 source
files because rg matched every file referencing "table".

Source files now only include files changed in the diff.
The agent investigates further files on its own using rg
and read, as the activation briefing instructs.

* fix(pr-review): scroll follows selection in list views

Add scrollToLine to WorkspaceInputContext so view handlers
can keep the selected item visible when navigating lists.
The method adjusts the scroll offset only when the target
line is outside the viewport, with a small margin.

Overview panel References and Source tabs, and review panel
comment lists all call scrollToLine after changing selection
or advancing after approve/reject.

* fix(pr-review): create worktree for PR branch during review

Without a worktree, the agent reads files from whatever
branch is checked out locally, which is usually not the PR
branch. This caused confusion during analysis — the agent
was reading code from a different branch than the PR.

After crawling context, check if the current branch matches
the PR head. If not, fetch the PR ref and create a worktree
at .review/pr-review-<number>. The session repoPath points
at the worktree so all file reads and rg searches see the
PR's code. Clean up the worktree on deactivate.

The activation briefing now shows the worktree path and
directs the agent to read files from it.

* fix(pr-review): improve references tab display

Show full titles instead of bare "#123" by enriching
references with titles and descriptions from already-fetched
issues and related PRs after the crawl completes.

Replace verbose "from PR body depth 0" with a compact depth
tag (ᐩ1, ᐩ2) only when depth > 0, and expand a description
below the selected item showing the issue/PR body preview
and source attribution — matching the Source tab pattern.

* fix(pr-review): make synopsis prompt conversational

The synopsis was reading like a dry commit message — listing
every file and change mechanically. Update the briefing and
skill instructions to ask for a conversational, teammate-
style summary that leads with motivation and explains the
approach approachably.

* fix(pr-review): fetch titles for bare issue/PR references

References extracted from PR body text only had bare "#123"
titles because they weren't in closingIssuesReferences.
After the local enrichment pass, fetch titles and body
previews via gh API for any references still showing bare
numbers. Limited to 10 lookups to avoid excessive API calls.

* fix(pr-review): AI reference summaries, drop emojis, fix ctx crash

Three changes:

Remove emoji type icons from the References tab. Items now
show just the title and depth tag.

Add reference_summaries parameter to generate-comments so the
agent writes one-sentence summaries for each reference. These
replace the raw body preview in the expanded description below
the selected item. The briefing lists references with URLs so
the agent can match them.

Fix "ctx is not defined" crash when opening the review panel.
The showReviewPanel parameter was renamed to _ctx by the
linter but the body still referenced ctx.

* fix(pr-review): fix remaining ctx references in review panel

buildDescTab, buildScopeTab, and buildFileTab all had their
parameter renamed to _ctx by the linter, but their bodies
pass ctx to buildCommentsView/buildFileCommentsView. The
previous fix only caught showReviewPanel — these three
inner functions had the same issue.

* fix(pr-review): absolute worktree path, no tool mentions

The worktree path was relative (.review/pr-review-N) which
caused fs.readFileSync to fail in raw mode since the cwd
might not be the repo root. Now resolves to an absolute path
via git worktree list.

Add instruction telling the agent to never mention tools
(rg, read, bash) or investigation process in comment text —
write as a human reviewer would.

* fix(pr-review): handle 'h' key inline without closing panel

The handled action was in globalActions, so pressing 'h'
closed the panel and returned an action result. Now 'h' is
intercepted by an injected input handler on every view that
calls markTabHandled and invalidates — the tab updates to
complete status without leaving the panel.

The HANDLED_ACTION stays in globalActions for hint bar
rendering, but the view handler catches the key first.

* fix(pr-review): clamp comment lines to diff hunk ranges

GitHub's review API returns 422 when comment line numbers
fall outside diff hunks. The agent generates comments with
absolute file line numbers that can extend past hunk
boundaries.

Build hunk ranges (newStart to newStart+newCount-1) per
file and clamp comment start/end lines to the nearest hunk
boundary. Lines within any hunk pass through unchanged.

* fix(pr-review): include comment context in steer feedback

When pressing 's' on a comment, the steer feedback only
included the user's note with no indication of which comment
was selected. The agent had to guess.

Now stash the selected comment's ID and subject when 's' is
pressed. The steer result carries this context, and the
handler builds a briefing that includes the comment ID, file,
lines, label, subject, and discussion — with instructions to
use update-comment with that specific ID.

* fix(pr-review): make verdict prominent in submit panel

The verdict was a small inline "Verdict: APPROVE" buried in
the middle of the panel. Replace it with a bold colored
banner at the top: ━━ APPROVE ━━ or ━━ REQUEST CHANGES ━━
in the verdict's color (green/red/accent), with the PR
reference in dim alongside it.
---COMMIT_END---
c5d371ff0789e8ade64b89d47ba4753c0104050c|2026-03-16 22:23:47 -0400|fix(pr-review): harden handlers and batch sibling PR fetches (#42)
Five fixes to the PR review extension:

- ensureContext re-fetches sibling PRs on session restore
  instead of passing an empty array, which silently lost
  related PR data after reload.
- handlePost rejects when draft comments exist, preventing
  silent submission with zero comments when the user skips
  vetting.
- handleResume re-shows the correct panel for context,
  description, and vetting phases instead of returning a
  plain text fallback.
- handleActivate rejects the graphqlReady promise when the
  GraphQL fetch throws, so dependent tasks (sibling PRs,
  previous reviews) fail fast instead of hanging forever.
- fetchSiblingPRs batches all linked issue numbers into a
  single GitHub search query using OR syntax instead of
  making one API call per issue.
---COMMIT_END---
4b75412aeea367cae7b6d25d29fc2bcaf15b7d38|2026-03-16 22:06:46 -0400|docs(pr-review): document absence of enforce.ts
Added a design note explaining why pr-review has no
enforcement file. The review workflow is read-only and
doesn't need write protection — the mode tracks phase
for UI display and context injection only.

---COMMIT_END---
e298ae15adc91e1f03e7defefe11e484e35d4370|2026-03-16 22:05:49 -0400|fix(pr-reply): render markdown in review overview panel
The review body and analysis text were pushed as raw strings
instead of being processed through renderMarkdown. This meant
markdown formatting (bold, lists, code blocks) in reviewer
comments and LLM analysis was displayed as plain text.

---COMMIT_END---
f03c07420f5456aa35ae7bd940c9598cd8d46b92|2026-03-16 22:05:04 -0400|refactor(pr-reply): type GraphQL responses in github.ts
Added typed response interfaces (ThreadsResponse,
ReviewsResponse, RefreshThreadsResponse) and made
runGraphQL generic, matching the pattern in pr-review's
API layer. Eliminates Record<string, unknown> casts and
deeply nested optional chaining on untyped data.

---COMMIT_END---
ba725d4dcb2d5db99532f1507afec3f25eb249df|2026-03-16 22:03:30 -0400|refactor(pr-review): use top-level imports in postReview
Replaced dynamic import() calls for node:fs, node:path,
and node:os with top-level imports. Per project conventions,
dynamic import is only for intentional lazy loading.

---COMMIT_END---
5be6971fbcfb58795f923c18d324be58606f58ad|2026-03-16 22:02:50 -0400|refactor: extract shared PR reference parsing to lib
Moved PRReference, parsePRReference, and extractOwnerRepo
to lib/parse/pr-reference.ts. Both pr-review and pr-reply
now re-export from the shared module instead of maintaining
separate copies.

Also fixed an inconsistency where pr-reply required a '#'
prefix for bare PR numbers while pr-review accepted both
'123' and '#123'. The shared version accepts both.

---COMMIT_END---
cfc6cf97478b2c117d32c8df5649d196bf62e4c4|2026-03-16 22:01:06 -0400|chore(pr-review): remove deprecated next-file action
The next-file action was deprecated in favor of the tabbed
review-files panel. Removed from the ACTIONS array, switch
statement, and action description to stop wasting prompt
tokens on a dead code path.

---COMMIT_END---
358646e57095461c970ad3b7208e831a59c1091d|2026-03-16 21:59:43 -0400|refactor(pr-reply): extract handlers and UI from index.ts
The index.ts was ~680 lines of inline handler logic, UI
rendering, and helpers — violating the convention that
index.ts should read as a table of contents.

Extracted to:
- handlers.ts — all action handlers as standalone functions
- briefing.ts — pure text-building functions for LLM context
- navigation.ts — PR resolution, code context, git push
- ui/thread-gate.ts — thread decision panel
- ui/reply-review.ts — reply approve/reject panel

The index.ts now only has registration and wiring: tool
registration, command/shortcut registration, event handlers,
and the action switch statement. Matches the pattern
established by pr-review's handlers.ts extraction.

Also cleaned up stale/duplicate JSDoc comments that were
left from incomplete refactoring.

---COMMIT_END---
a67fa99860c53c75983b928034d716ecf06391ec|2026-03-16 20:55:59 -0400|refactor(pr-review): extract handlers, type comment param
Move all 13 action handlers from index.ts to handlers.ts.
Each handler receives a HandlerDeps bundle (state + pi)
instead of closing over the parent scope. Index.ts is now
267 lines of pure registration and wiring — tool definition,
commands, shortcuts, event handlers.

Type the comment parameter: define CommentInput interface
matching the TypeBox schema. Handlers receive CommentInput
instead of unknown, eliminating all String()/Number() casts
and the Record<string, unknown> workaround.

HandlerDeps bundles state and pi for clean dependency
passing. Module-level helpers (textResult, getCurrentRepo,
resolvePR, ensureContext, applyVettingDecisions,
formatGitHubComment) moved to handlers.ts alongside the
handlers that use them.

---COMMIT_END---
0f0db78fd8f0e99690a474420b758c73cfdd9ad2|2026-03-16 20:48:51 -0400|docs(pr-review): update skill for new workflow
Rewrite the pr-review skill to reflect the redesigned
extension:

- Tabbed file review replaces sequential wizard. No more
  next-file action — the user navigates files freely with
  three views per file (diff, file, comments).
- Comment management actions: add-comment, update-comment,
  remove-comment with comment_id parameter.
- Description panel is interactive — steer to request
  comments on title, description, or scope.
- Vetting panel posts directly — user presses 'p' from
  summary tab. Steer to edit verdict or review body.
- Conversation breakout from any panel via Shift+Enter,
  resume re-opens the current phase's panel.

---COMMIT_END---
349b1ded2bc78087b98ff1ebacb3ee1a83ac6441|2026-03-16 20:47:53 -0400|feat(pr-review): extend vetting and add comment management
Vetting panel improvements:
- Summary tab now has 'p' to post review directly from the
  panel, eliminating the separate post action round-trip
- Steer from summary tab guides LLM to edit verdict or body
- Steer from comment tab guides LLM to use update-comment
- Hint text in summary tab tells user about Shift+Enter

New actions for comment lifecycle:
- update-comment: edit an existing comment by ID with partial
  updates (any subset of fields)
- remove-comment: delete a comment by ID

Both accept a comment_id parameter. The vetting steer path
now references update-comment instead of the broken pattern
of adding duplicates via add-comment.

Updated promptGuidelines to reflect the new workflow: tabbed
file review, comment management actions, and direct posting
from the vetting panel.

---COMMIT_END---
b4a3c211c652fe30e6da6f023baad8b057a47063|2026-03-16 20:43:48 -0400|feat(pr-review): rebuild file review as tabbed panel
Replace the sequential one-file-at-a-time wizard with a
tabbed panel using the new views API. Each file is a tab
with three views:

  d — Diff: unified diff with comment summary
  f — File: full file from worktree, syntax highlighted,
      lazy-loaded on first view switch
  c — Comments: detailed list with status, labels, and
      discussion text

The user navigates files with Tab/Ctrl+#, switches views
with d/f/c, steers to request comments, and submits when
done. No more next-file action — file navigation is
user-driven within the panel.

The next-file action is kept as a graceful redirect that
tells the LLM to use review-files instead.

Remove fileIndex from state (tabbed panel handles it).
Remove directory grouping helpers (tabs handle large PRs).
Simplify detail widget for files phase.

Update briefings: briefFile/briefFileSteer replaced with
briefFileReview (summary after completion) and simplified
briefFileSteer (just file path and note).

---COMMIT_END---
5fda7d6fe948c46206f36b6756741f941579b062|2026-03-16 20:38:56 -0400|feat(ui): add sub-view support to tabbed prompts
Extend the shared prompt library with content views. Each
PromptItem now has a views array instead of a direct content
field. Each PromptView has a key, label, and content function
(sync or async for lazy loading).

Items with multiple views show view key-hints in the hint bar
using [K]eyword formatting. The active view is highlighted in
accent; inactive views are dim. Items with one view show no
view hints. Pressing a view's key switches the content area
and resets scroll.

Async content support: when a view's ContentFn returns a
Promise, the panel shows "Loading…" while awaiting, then
caches the result for subsequent renders.

New types: PromptView, AsyncContentFn. Updated PromptItem to
use views array. Panel-layout buildHintBar accepts view hints.
Prompt-tabbed tracks activeViewIndex per tab with per-view
scroll state and content cache.

All consumers migrated: pr-review (context-summary, vetting,
file-review), pr-annotate (vet), plan-mode (interview),
tdd-mode (refactor-gate), ask. Each wraps its content in a
single-element views array.

---COMMIT_END---
ce7c39cbd8a2313cb0671949820c2d04bcd220fd|2026-03-16 20:18:02 -0400|feat(pr-review): make description panel interactive
The description review panel was decorative — its return value
was ignored and the handler always returned the same text.

Now returns a discriminated union: continue / steer / cancel.
The handler branches on each:
- continue → briefDescription (evaluation checklist for LLM)
- steer → briefDescriptionSteer (user feedback + context for
  LLM to draft description/scope comments)
- cancel → dismiss message with re-show instructions

This makes the description panel an interactive decision point
where the user can tell the LLM they want comments on the
title, description, or scope before proceeding to analysis.

---COMMIT_END---
76ccc4649d97aaef0723f5594a044c5e5d23c33f|2026-03-16 20:15:34 -0400|refactor(pr-review): extract LLM briefings
Move all text-for-the-LLM out of action handlers into
briefing.ts — pure functions that take review data and
return markdown. No side effects, no state mutation, no UI.

Functions: briefActivation, briefContext, briefDescription,
briefAnalysis, briefFile, briefFileSteer. The analysis
briefing is decomposed into focused sub-sections:
appendIssueContext, appendPRDiscussion, appendDiff,
appendPreviousThreads, appendInvestigationInstructions.

Magic numbers extracted as named constants: MAX_DIFF_CHARS,
MAX_ISSUE_COMMENTS, MAX_COMMENT_PREVIEW, MAX_THREAD_PREVIEW.

Each handler now reads as: guard → operate → brief. The two
audiences (user sees panel, LLM reads markdown) are cleanly
separated. index.ts drops from 1,341 to 965 lines.

---COMMIT_END---
e6d7671254dbad7af86be4ec70d3318fe0422cc0|2026-03-16 20:11:09 -0400|refactor(pr-review): type GraphQL response layer
Define typed interfaces for both GraphQL query responses in
api/types.ts (PRContextResponse, ReviewsResponse) with all
nested node types (GQLPullRequest, GQLIssue, GQLReview,
GQLReviewThread, etc.).

Rewrite api/github.ts to use typed responses. The runGraphQL
function is now generic — runGraphQL<T>() returns the typed
shape. Parsing functions receive GQLPullRequest directly
instead of navigating unknown objects.

Remove nested(), str(), num() helpers and their ~15 cast
sites. All property access is now direct field reads on
typed interfaces.

Fix resolvedBy detection in fetchPreviousReviews — the
REVIEWS_QUERY never fetched the PR author, so the old
nested() path always returned undefined. Now accepts the
PR author login as a parameter from the caller, where it's
already known from the context query.

---COMMIT_END---
1b3f429630218d8fa74de62d97a401d047247e81|2026-03-16 20:08:12 -0400|refactor(pr-review): introduce domain model
Replace the flat 20-field state bag with structured domain
types that read like a description of what a code review is.

PRTarget — identifies the PR (owner, repo, number, branch,
baseBranch, author). Replaces six nullable fields.

ReviewSession — the active review with context, worktree,
comments, body, verdict, and previous review data. Comments
now carry inline status (draft/accepted/rejected) instead
of a parallel commentStates map.

PreviousReviewData — bundles reviews and threads into a
single nullable field, replacing three separate fields
(isReReview, previousReviews, previousThreads).

PRReviewState — now just four fields: enabled, session,
phase, fileIndex.

Comment helpers as pure functions: addComment, updateComment,
removeComment, commentsByStatus, commentsForFile. These
operate on the session directly.

Lifecycle persistence simplified: one PersistedSession shape
instead of 20 individual fields. Restore creates a session
with null context (re-fetched on demand).

All handlers updated to read from state.session instead of
scattered fields. Non-null assertions replaced with proper
guards throughout.

---COMMIT_END---
86a80f2a421bc79fc0ad2ad13a5a3bb14ad822aa|2026-03-16 20:00:36 -0400|refactor(pr-review): remove dead code
Remove fields, types, and code paths that were declared but
never connected:

State fields: analysis, commentIndex, descriptionComments,
researchNotes — all persisted and restored but never written
to or meaningfully read.

Context fields: parentIssues, externalLinks — always empty
arrays in assembleContext. ExternalLink type removed entirely.

CommentState "edited" — defined in union but no code path
ever set it. Removed from type and from the post filter.

ReviewComment.source — always "llm", never branched on.

Links tab in context-summary — checked for data that could
never exist. Removed along with dead action loop that
returned true regardless.

Orphaned JSDoc above DIRECTORY_GROUP_THRESHOLD constant.

---COMMIT_END---
498009602c26824623e4411632c96f0988fe19da|2026-03-16 19:58:12 -0400|refactor(ui): extract progress panel to shared library
Move the live progress panel from pr-review/ui/progress.ts to
lib/ui/progress.ts as a shared component. The panel runs
parallel async tasks with real-time status updates and Escape
to cancel.

Clean interface: progress(ctx, config, tasks) returns results
in order or null on cancel. Composes GLYPH from types.ts and
truncateToWidth from pi-tui. Preserves the non-interactive
sequential fallback.

Renamed internal status from "fetching" to "running" for
clarity. Separated glyph color and label color into distinct
maps. Removed pr-review-specific FetchTask naming in favor
of generic ProgressTask.

---COMMIT_END---
57ea359f51663f3d4886dc38823d7509acd2bc13|2026-03-16 18:36:55 -0400|feat(pr-review): re-fetch context on session restore (#40)
When a session restarts mid-review, the GatheredContext is lost
because it's too large for appendEntry. Previously every action
would fail with "No PR review active."

Now an ensureContext helper transparently re-fetches the PR
metadata and diff from GitHub when context is missing but the
review is still enabled. The user sees a brief "Re-fetching"
notification and continues where they left off.
---COMMIT_END---
5033710765706b37f1194e3e83ea818985bd8ef1|2026-03-16 18:25:12 -0400|Add Keyboard Shortcut, Directory Grouping, and Comment Editing (#39)
* feat(pr-review): add keyboard shortcut and directory grouping

Adds Ctrl+Alt+V keyboard shortcut and /pr-review command to
deactivate PR review mode (activation still requires the tool
since it needs a PR reference).

For PRs with 15+ changed files, the first file review includes
a directory overview showing how changes are distributed across
directories with per-directory stats.

* feat(pr-review): support comment editing via steer in vetting

When the user steers from a comment tab in the vetting panel,
the feedback and comment context are returned to the LLM with
instructions to draft an updated version via add-comment, then
re-show vetting.

This completes the edit flow: the user sees a comment they want
to change, steers with natural language ("make this non-blocking"
or "add more context about the pattern"), the LLM updates it,
and vetting re-opens with the revised comment.
---COMMIT_END---
aa4c8c7db62c3c3003c5edd5e11e67403b6e2332|2026-03-16 17:21:24 -0400|feat(pr-review): add re-review support with previous thread tracking (#38)
Detects when the user has previously reviewed a PR and enriches
the workflow with previous review context:

- Fetches previous reviews and threads by the current user
  during activation (parallel progress task)
- Tracks resolution state: who resolved each thread (self,
  author, or other)
- Integrates into deep analysis with categorized thread lists:
  self-resolved (filtered), author-resolved (assess satisfaction),
  open (check if new changes resolve them)
- Adds re-review summary to activation output and context injection
- Persists/restores re-review state across sessions
---COMMIT_END---
667dcc772e1ea9ba5af54898b95f518d278c7bcd|2026-03-16 10:40:58 -0400|feat(pr-review): enhance comment flow with steer-to-add pattern (#37)
Enriches the steer return during file review with explicit
instructions for the LLM to draft a conventional comment and
call add-comment. Includes the worktree path for investigation
and references the conventional-comments skill.

The resume action now re-shows the file review panel when
returning during the files phase, so the user sees the updated
comment list immediately after a comment is added.

Also persists state after add-comment and includes a "call
resume" instruction in the response.
---COMMIT_END---
b1c8ea0035d99992edb3b0648adc9b8c0b11f824|2026-03-16 00:45:15 -0400|chore(pr-review): polish references and comment filtering (#36)
Updates AGENTS.md to reflect the new pr-review tool and
pr-review/ directory in the tool extensions list.

Fixes handlePost to only submit accepted/edited comments —
draft comments that were never vetted are now excluded.

Adds .review/ to .gitignore so worktree directories created
during PR review are not tracked.
---COMMIT_END---
a59a5b0639e95e5727a12ff8205078f84263139d|2026-03-16 00:43:35 -0400|feat(pr-review): add final vetting panel with per-comment tabs (#35)
Tabbed vetting UI where each comment gets its own tab plus a
summary tab. Users approve or reject each comment individually.
The summary tab shows the draft review body, verdict, label
breakdown, and file coverage stats.

The vetting result feeds directly into the post action — only
accepted comments are submitted to GitHub. The suggested verdict
is inferred from whether any comments have blocking decorations.
---COMMIT_END---
67ff0dbd948fdf609802bdda5202570ff66a82d5|2026-03-16 00:40:59 -0400|feat(pr-review): add file review panel with diff display (#34)
Shows a visual diff panel for each file during file-by-file
review. The panel renders the unified diff with syntax coloring,
displays existing comments for the file, and shows the worktree
path for full-file access.

User actions: 'n' for next file, Shift+Enter to steer (add
comments or discuss), Escape to pause. Steer feedback is
returned to the LLM with the file's diff context.

The panel is shown first for the human, then text context is
returned to the LLM — same dual-output pattern as the context
summary and description panels.
---COMMIT_END---
62336dfa9903629c7e8fed20bf63c6ff6ed39a19|2026-03-16 00:38:34 -0400|feat(pr-review): enrich deep analysis with full context (#33)
The analyze action now returns the full diff, issue bodies with
comments, PR discussion threads, and targeted investigation
instructions. The LLM gets everything it needs to perform
thorough analysis using bash and read tools.

Includes concrete rg search suggestions extracted from added
function/class names in the diff, plus per-category instructions
for test coverage, implementation quality, and consistency.
---COMMIT_END---
4ff7e4c6a2ff289f08c0171aa189ac03d389c1f9|2026-03-16 00:35:57 -0400|docs: add pr-review, code-review-criteria, and conventional-comments skills (#32)
Three skills that teach the LLM how to review PRs:

pr-review: The workflow skill — action sequence, what to do
at each phase, how to use the pr_review tool, and when to
break out to conversation.

code-review-criteria: What to evaluate — test quality (behavior
vs implementation, idiom, coverage), implementation quality
(readability, abstraction, naming), consistency (pattern search,
conventions), scope, and description accuracy. Loadable
independently for any code review context.

conventional-comments: How to format feedback — labels (praise,
suggestion, issue, question, etc.), decorations (blocking,
non-blocking), tone guidance (Canadian-polite, instructional),
and evidence-based commenting. Reusable across pr-review,
pr-reply, pr-annotate, and casual feedback.
---COMMIT_END---
2bbbef4fba13d86df0313784939f8ba3f18ab8d8|2026-03-16 00:33:22 -0400|feat(pr-review): add context summary and description panels (#31)
Two new UI panels shown during the review workflow:

Context summary (multi-tab): Overview, Issues, PRs, and Links
tabs showing all gathered PR context. Uses the shared tabbed
prompt component with a Continue action.

Description review: Shows PR title, body, scope stats, and
linked issues for the user and LLM to evaluate together.

Both panels show visual content then return text context to
the LLM for analysis, so the human sees a rich display while
the LLM gets structured data to reason about.
---COMMIT_END---
14556891ed44eed6bb91d3274d724ab0e46ea51d|2026-03-16 00:30:23 -0400|feat(pr-review): add live progress panel for context gathering (#30)
Shows a real-time panel during PR activation with status glyphs
for each fetch task (metadata, diff, sibling PRs). Tasks run
in parallel with coordinated dependencies — sibling PR search
waits for the metadata task to provide linked issues.

The panel uses Pi's ctx.ui.custom() with tui.requestRender()
to update as each task completes. Escape cancels all tasks.

Also refactors api/github.ts to export individual fetchers
instead of a monolithic fetchPRContext, enabling fine-grained
progress tracking.
---COMMIT_END---
f61482d4c8490020d49bcfeda42209af7048848f|2026-03-16 00:25:45 -0400|feat(pr-review): scaffold extension with core workflow (#29)
New pr_review tool for reviewing someone else's pull request.
Implements the full action-driven workflow: activate, gather
context, review description and scope, deep analysis, file-by-
file review, comment collection, vetting, and posting.

Modules follow the mode file convention:
- state.ts — types, defaults, comment lifecycle
- lifecycle.ts — activate, deactivate, persist, restore, UI
- transitions.ts — context injection and filtering
- worktree.ts — git worktree create/cleanup for isolation
- api/parse.ts — PR reference parsing (owner/repo#number)
- api/github.ts — GraphQL/REST fetching, diff parsing, posting
- index.ts — tool registration and action handler wiring
---COMMIT_END---
d750587eafc0d1034c32132b8794862d524863b9|2026-03-16 00:17:40 -0400|refactor: rename pr-review to pr-annotate (#28)
Clear the pr-review name for the upcoming PR review extension.
The existing self-review annotation tool becomes pr_annotate
to distinguish it from full PR review workflows.

Renames the extension directory, tool name, and all references
in AGENTS.md and the pr-writing skill.
---COMMIT_END---
87a1f923b5ca2c0fb60140fd0cd50e991bfb10b6|2026-03-15 22:59:05 -0400|feat(google-workspace): add unarchive_email action (#27)
Archiving removes the INBOX label, but there was no way to
reverse it. Add unarchive_email to move emails back to the
inbox by adding the INBOX label.

Also fix ensureAuthenticated to pass oauthConfig through to
the getAuthClient callback, matching its actual signature.
---COMMIT_END---
7a8afc1d5593f06f9f8168032a654596feff1c82|2026-03-15 02:12:59 -0400|Persist Google Workspace Credentials and Polish Auth UX (#26)
* feat(google-workspace): persist credentials to disk

Credentials were stored in Pi's session state, which is lost
when starting a new session. Now they're written to
~/.pi/agent/google-workspace.json so OAuth app config, account
list, and tokens survive across Pi restarts.

This also simplifies the credentials API — functions no longer
need ExtensionAPI or ExtensionContext parameters since they
read/write a file directly.

* fix(google-workspace): use shared prompt for auth gate

The auth confirmation used ctx.ui.confirm which rendered
"undefined" for its empty message parameter. Replaced with
the shared prompt() panel that shows the explanation and
options together cleanly.

* fix(google-workspace): clear client cache on reset

/google-reset cleared the credentials file but left cached
OAuth clients in memory, so requests kept working until Pi
was restarted.

* refactor(google-workspace): use shared UI throughout auth

Replace raw ctx.ui.notify/confirm calls with the shared
prompt() and view() panels across the entire auth flow:
setup wizard, auth command, dual OAuth flow, and reset.

Add signal support to view() so panels can be dismissed
programmatically when async work completes.

Also adds:
- Auto-open OAuth URLs in the default browser
- Dark mode support on the callback page
- Auto-close on successful authentication
- Cross-platform browser launcher (open/xdg-open/cmd)

* docs(skills): codify PR review line number verification

Add explicit procedure to pr-writing skill for verifying
line numbers against diff hunks before calling pr_review.
GitHub rejects comments on lines outside diff context, and
guessing from file contents causes 422 errors.

* docs(skills): lowercase GitHub username in branch names

Branch naming convention requires lowercase, but the
username from gh api may have mixed case. Pipe through
tr to lowercase automatically.
---COMMIT_END---
c42ae2635ff698359bf19385a57be9900c97d58a|2026-03-15 01:34:12 -0400|Add Component Library and Migrate All Extensions (#25)
* feat(ui): add component library primitives

Introduces 6 new components and a types module for the
declarative prompt/view component library:

- types.ts: public API types, glyph vocabulary, layout
  constants (GLYPH, ContentFn, Action, Option, PromptResult,
  TabbedResult, SinglePromptConfig, TabbedPromptConfig)
- scroll-region.ts: content viewport with vertical/horizontal
  scrolling, scrollbar rendering, and height budget
- action-bar.ts: key-hint action bar with hold-to-reveal
  steer layer using Kitty keyboard protocol
- option-list.ts: numbered vertical option list with cursor
  and on-select descriptions
- tab-strip.ts: scrolling tab bar with status glyphs, progress
  bar, and Ctrl+number jump
- note-editor.ts: inline editor wrapper with bordered styling
- panel-new.ts: layout orchestrator composing all components
  into prompt() and view() functions via ctx.ui.custom()
- steer.ts: formatSteer utility for converting steer notes
  into guardian block reasons

Named panel-new.ts to coexist with existing panel.ts during
the migration period. The old system continues to function
while consumers are migrated.

* refactor(ui): delegate renderMarkdown to Pi Markdown

Replace the hand-rolled markdown renderer with Pi's Markdown
component which provides full-featured parsing via marked:
tables, nested lists, proper code fence highlighting, and
blockquote rendering.

Remove auto-detection functions (renderContent,
detectContentType, detectContentTypeFromPath) from the
shared content-renderer. Callers know their content type and
call the specific render function they need.

Migrate content-viewer to use the new view() function and
explicit content type selection instead of auto-detection.

* feat(ui): add status utility functions

Add setModeStatus, setDetailWidget, and clearStatus helpers
for managing footer indicators and above-editor widgets.
Wraps Pi's ctx.ui.setStatus and ctx.ui.setWidget with a
consistent API that checks for UI availability.

* refactor(ui): migrate all extensions to prompt/view

Port every consumer from the old showPanel/showPanelSeries/
showGate/reviewLoop system to the new prompt() and view()
API with hold-to-reveal steer annotations.

Guardians (commit, PR, issue, history) now use prompt()
with actions instead of reviewLoop. The edit-loop pattern
is eliminated — steer annotations flow through the LLM
instead of manual text editing.

Mode extensions (plan-mode, tdd-mode) use prompt() with
actions for transitions and tabbed items for multi-item
reviews (interview questions, refactor suggestions).

Tool extensions (ask, pr-review, pr-reply) use prompt()
with options for single questions, tabbed items for multi-
question flows, and actions for thread decisions.

Google Workspace confirmation gates use prompt() with
actions for approve/cancel patterns.

Content viewer uses the new view() function for read-only
display.

* refactor(ui): remove old panel system, rename panel-new

Delete files replaced by the component library:
- lib/ui/panel.ts (old showPanel/showPanelSeries)
- lib/ui/panel-state.ts (absorbed into scroll-region)
- lib/ui/panel-render.ts (absorbed into components)
- lib/ui/panel-keys.ts (absorbed into components)
- lib/ui/gate.ts (absorbed into prompt + formatSteer)
- lib/guardian/review-loop.ts (steer replaces edit)

Rename panel-new.ts to panel.ts now that the old panel.ts
is removed. Update all consumer imports.

* feat(test-harness): add UI component validation commands

Registers /test-* commands that exercise every component in the
component library: ScrollRegion, ActionBar, OptionList, TabStrip,
NoteEditor, and the three content renderers (markdown, code,
diff). Each command reports its result via notify so the tester
sees exactly what the code received.

Intended for manual validation passes after UI migrations.

* fix(test-harness): use correct registerCommand API

The pi registerCommand API takes (name, { description,
handler }) but the test harness was using a non-existent
positional (name, description, handler) signature. Also
removes the "/" prefix from command names since pi adds
that automatically.

* fix(panel): add padding above view hint bar

The view component had no spacing between the last content
line and the hint bar (Esc close), but did have spacing
below the title. Adding a blank line before the hints
creates symmetric padding. Chrome budget adjusted from 2
to 3 for the extra line.

* fix(panel): replace hold-to-reveal with direct steer

The hold-to-reveal design relied on detecting bare Shift
press/release events via Kitty protocol. Pi requests
Kitty flags=7 which does not include flag 8 (report all
keys as escape codes), so modifier-only key events are
never sent. The display toggle on Shift hold never worked.

Additionally, Kitty flag 2 (report event types) causes
terminals to send both press and release events for
Shift+key. The release event was leaking into the
NoteEditor as a typed character.

Changes:
- Remove detectShiftTransition and renderSteerBar
- Remove shiftHeld state tracking from both prompts
- Filter release events via isKeyRelease() at the top
  of both single and tabbed prompt handleInput
- Action bar always shows steer hint inline
- Remove redundant steer hint from bottom hint bar
- Remove wantsKeyRelease from component returns

* fix(panel): shrink panels to fit content

Panels always padded content to fill 60% of terminal
height, creating large blank areas when content was
small (e.g., 3 lines of text with 13 lines of padding).

Now padding only activates when content overflows and
scrolling is active, keeping height stable during scroll.
Short content renders at its natural height.

* fix(option-list): correct description indent

The description indent used prefix.length which counts
ANSI escape bytes, not visible width. The accent-colored
cursor glyph made the indent far too wide. Use the fixed
visible width (4 characters) instead.

* style(action-bar): place steer hint after actions

The steer annotation hint was right-aligned, creating a
large gap on wide terminals. Move it immediately after
the action labels for better visual grouping.

* fix(panel): tab status and user item indicator

Two fixes for the tabbed prompt:

- Tab statuses now show completion state even when the
  tab is current. Previously the "active" glyph always
  took precedence, so actioning the last remaining tab
  showed no visual change (glyph stayed ◈, progress bar
  didn't update). Now completed tabs always show ◆/✕.

- Added "+N added" indicator next to the progress bar
  when user items have been added via the + key.

* fix(action-bar): use matchesKey for plain actions

Plain action matching used direct string comparison
(data === action.key) which only works when the terminal
sends the raw character. Using matchesKey handles both
legacy and Kitty CSI-u encoded key events.

* fix(panel): reserve scrollbar gutter in content width

Content was rendered at full terminal width but displayed
in width minus 2 columns when a vertical scrollbar was
present. This caused truncateToWidth to add "..." to every
line. Now content is always rendered at width minus the
scrollbar gutter, and the horizontal scroll check uses the
effective display width.

* fix(content-renderer): code highlight marker and trim

Highlighted lines used theme.fg("accent") which was
invisible over syntax-highlighted code because inner
ANSI colors override the outer wrap. Replace with an
accent gutter marker (▎) that's always visible.

Also trim trailing newlines before highlighting to
prevent an empty extra line at the end.

* style(panel): normalize indent to 1 space

Title, hints, action bar, tab strip, separator, and
option list all used 2-space indent while content
renderers (markdown, diff, code) used 1-space. This
created a visible misalignment. Normalize everything
to 1-space indent for consistency.

* fix(content-viewer): remove duplicate title

The content viewer rendered the filename both as the
panel title and inside the content body, causing a
double title. Remove the in-content duplicate since
the panel's view() already renders the title in chrome.

* perf(panel): cache content and hscroll computation

Three performance improvements:

- Content lines are cached per width in all panel types
  (view, single prompt, tabbed prompt). Content functions
  are only called when width changes.

- maxContentWidth (which calls visibleWidth on every line)
  is computed once when content is cached, not on every
  render. This was the main bottleneck — 12ms per render
  scanning 800+ ANSI-heavy lines.

- Code syntax highlighting is pre-computed before the panel
  opens via preHighlightCode, separating the expensive
  highlighting from the fast formatting pass.

Also fixes multi-line syntax coloring (comments, strings)
by tracking ANSI escape state across lines. The gutter's
dim color was resetting the carried-over highlight state,
causing continuation lines to lose their color.

* feat(content-renderer): expand tabs in code rendering

Tabs in syntax-highlighted code were rendered at the
terminal's default width (usually 8 spaces). Now tabs
are expanded to spaces during formatting, defaulting
to 3 spaces. Callers can override via tabWidth option.

* fix(guardians): use panel title for consistent styling

All four guardians were rendering their own title inside
the content function with inconsistent styles (dim color,
no label, etc). Moved to the panel's title config so they
all render with the standard accent+bold styling used by
view panels and test commands.

- Commit: "Commit" / "Amend Commit"
- History: "Destructive Command" / "Risky Command"
- PR: "New PR" / "PR Edit"
- Issue: "New Issue" / "Issue Edit"

* fix(tab-strip): scale progress bar to item count

The progress bar was hardcoded to 5 blocks regardless of
the actual item count. With 3 items and all completed, it
showed [▓▓▓░░] 3/3 — two empty blocks that shouldn't
exist.

Now the bar width matches the item count when total <= 5,
and scales proportionally when total > 5.

* fix(tdd-mode): replace emoji glyphs with themed indicators

Phase indicators used literal emojis (🔴🟢🔄⏹) which
looked inconsistent with the rest of the UI. Replaced with
themed "●" glyphs colored by phase (error/success/accent).

Also fixed phase names in transition gates from ALL CAPS
(RED, GREEN, REFACTOR) to title case (Red, Green, Refactor)
for consistency with other panel labels.

Added panel title to transition gates so they render with
the standard accent+bold styling.

* fix(tdd-mode): use descriptive transition action labels

Changed action label from bare phase name ("Green") to
"Transition to Green" so the action's intent is clear.

* fix(panel): improve editor labels for steer and actions

- Changed "Steer:" to "Feedback:" for pure steer editor
  label — clearer intent.
- Action steer label already shows the action name
  (e.g. "Move to Green with note:").
- Updated in both single and tabbed prompt variants.

Also shortened TDD transition action from "Transition to"
to "Move to" for conciseness.

* fix(action-bar): use Shift+Enter for pure steer

Pure steer was Shift+S, which conflicted when an action
used key "s". Any letter key could have the same problem.

Moved pure steer to Shift+Enter — a modifier combo that
can never conflict with action letter keys. Action steers
are now checked before the pure steer fallback.

Both hints always appear on the action bar since both are
always available when actions are shown.

* fix(tab-strip): show tab numbers and user item labels

Two UX improvements to the tab strip:

1. Tab numbers are now visible next to each tab label
   (e.g. "◇ 1 Q1") matching the Ctrl+# jump shortcut.
   Hint updated from "Ctrl+N jump" to "Ctrl+# jump".

2. User-added items now appear as labeled tabs instead
   of just "+N added". Each shows a truncated preview
   of the item text (up to 12 chars) with a complete
   status glyph, so users can see what they added.

* fix(tab-strip): make labels optional, numbers always show

Tab numbers always display for positional context and
Ctrl+1-9 jump discoverability. Labels are optional —
when empty, only the number shows (e.g. "◇ 1"). When
meaningful, both show (e.g. "◇ 1 Priority").

Tabs beyond 9 still show numbers for orientation even
though Ctrl+# shortcuts cap at 9. Tab/arrow keys
navigate all tabs.

Updated test harness tab labels from generic "T1"–"T5"
to meaningful names: Actions, Options, Scroll, etc.

* fix(status): replace emojis with themed geometric glyphs

All status line indicators now use the same geometric
glyph vocabulary as the rest of the UI:

- TDD: phase-colored ● + dim "TDD" (color tracks phase)
- Plan: warning-colored ◈ + dim "Plan"
- PR Reply: accent-colored ◈ + dim "PR #N"

Consistent with the ◆◇◈● glyph system used in tab
strips, option lists, and transition gates.

* fix(scroll): clamp vOffset on scroll-down to prevent over-scroll

handleScrollInput incremented vOffset without clamping, so
pressing Shift+Down past the end accumulated invisible offset.
Scrolling back up then had to unwind that over-scroll before
visually moving.

Pass contentLength to handleScrollInput and clamp via
clampVScroll on every downward scroll. All three call sites
(view, prompt-single, prompt-tabbed) now supply their cached
content length.

* fix(scroll): gate horizontal scrolling on needsHScroll

Shift+←→ was handled unconditionally, allowing content to scroll
off-screen even when it fit within the viewport width. Pass the
existing needsHScroll flag through to handleScrollInput so
horizontal scroll keys are ignored when content fits.

* chore(test-harness): add titles to prompt-based test commands

Adds themed title lines to test-actions, test-options,
test-steer, and test-editor so each panel is identifiable
during validation.

* feat(tabs): replace per-user-item tabs with aggregate user tab

Instead of each user item getting its own truncated tab in the
strip, a single +N tab appears when canAddItems is enabled. The
tab shows a list of user items as an option list with an action
bar for [A]dd, [E]dit, and [D]elete.

- tab-strip: replace userItems array param with userItemCount
  number; append single +N tab when count >= 0; exclude user
  tab from progress total
- panel (tabbed): virtual user tab at index config.items.length
  with dedicated input handling, option navigation, and content
- action-bar: add showSteerHint param to suppress steer hints
  on the user tab
- Remove global + hotkey — adding items is now done from the
  user tab itself

* feat(tabs): accent Ctrl+Enter hint when all items are resolved

buildHints becomes buildHintBar — returns a pre-styled string
instead of plain text array. Each hint is individually styled,
and the Ctrl+Enter submit hint switches from dim to accent when
results.size >= config.items.length.

* fix(tabs): remove redundant empty-state text on user tab

The action bar already shows [A]dd — the instructional sentence
was unnecessary.

* fix(pr-reply): pass pi instead of ctx to findPRForBranch

resolvePR was passing ExtensionContext where ExtensionAPI was
expected, causing pi.exec is not a function at runtime.

* fix(pr-reply): render code context as dim text instead of raw fences

The code context from readCodeContext already has line numbers
and markers. Wrapping it in literal ``` fences just showed
them as raw characters. Render each line with dim styling instead.

* fix(pr-reply,tdd-mode): correct renderCall signature to (args, theme)

renderCall takes (args, theme) per the API, but pr-reply had
(args, _options, theme) and tdd-mode had (args, options, theme).
The extra parameter shifted theme to the third position where
it received undefined, causing renderCall to crash. Pi caught
the error and fell back to default rendering, creating visual
artifacts like duplicate Working spinners.

* fix(pr-reply): render code context with syntax highlighting

Replace raw dim text with renderCode for proper syntax
highlighting, line numbers, and tab-to-space conversion.
readCodeContext now returns structured data (source, startLine,
highlightLine, language) instead of pre-formatted text.

* refactor(pr-reply): convert option lists to action bars in panels

Summary, review overview, and rebase panels used vertical option
lists where compact horizontal action bars suffice. Saves
vertical space.

* feat(google-workspace): support steer annotations on all confirmations

All five confirmation gates (send email, delete email, create
event, update event, delete event) now return ConfirmResult<T>
instead of raw data or boolean. Steer annotations return
formatted feedback so the agent can adjust and retry.

* fix(google-workspace): extract reason from steer result

formatSteer returns { block, reason } but extractSteer was
passing the whole object as content text. Pi core calls
stripAnsi on content text, which throws when it receives an
object instead of a string — crashing the session.

Use .reason to unwrap the string, matching how pr-reply
already handles this.

* refactor(ui): split panel.ts into focused modules

Extract the two prompt implementations and shared layout
helpers into separate files:

- panel.ts — public prompt/view entry points (164 lines)
- prompt-single.ts — single prompt variant (296 lines)
- prompt-tabbed.ts — tabbed prompt variant (537 lines)
- panel-layout.ts — computeChromeLines, buildHintBar (58 lines)

No circular imports: panel imports from prompt-single and
prompt-tabbed, both import from panel-layout. External
callers still import prompt/view from panel.js unchanged.

* chore(test-harness): add horizontal scroll test command

Adds /test-hscroll with wide TypeScript code that exceeds
terminal width, exercising Shift+←→ horizontal scrolling
and ANSI-aware slicing through syntax-highlighted content.

* fix(ui): gate horizontal scroll behind allowHScroll opt-in

Previously, horizontal scrolling was auto-enabled whenever
content exceeded viewport width, meaning markdown panels,
guardian gates, and other text-heavy views got Shift+←→
scrolling. This is wrong — horizontal scroll should only
apply to code and diff content.

Add allowHScroll to SinglePromptConfig, TabbedPromptConfig,
PromptItem, and ViewConfig. Default is false. Only callers
showing code content opt in: content-viewer (code/diff),
pr-reply (thread view with code context), pr-review (vet
items with code snippets), and test harness code commands.

* fix(scroll): keep 3-line overlap when paging up/down

Scrolling by the full viewport budget lost all visual
context between pages. Now pages by budget minus 3 lines,
keeping the last 3 lines visible as overlap for continuity.

* chore(ui): remove unused status.ts module

The setModeStatus, setDetailWidget, and clearStatus helpers
were never imported by any extension. All callers use
ctx.ui.setStatus directly with their own themed strings —
TDD mode colorizes by phase, plan mode uses warning accent,
pr-reply uses accent glyph. Each mode needs different
styling, making a shared helper the wrong abstraction.

* style(status-line): improve visual hierarchy in footer

- Last path component in blue-gray (ANSI 103) as the
  visual anchor for "where am I"
- Branch separated by · instead of parentheses, dim
- Model name dim — secondary information
- Mode status labels use muted to pair with colored glyphs
- Context tokens turn warning above 80% usage
---COMMIT_END---
f86ac5b01756e54a314f48d80bf25a3df62c0737|2026-03-14 12:47:49 -0400|docs(skills): use gh cli for branch username prefix (#24)
Replace the heuristic of inferring the username from existing
branches with a direct `gh api user --jq .login` call. The
authenticated GitHub user is always the correct prefix.
---COMMIT_END---
f72ecfed35c785493737141f9f546cbd33085de7|2026-03-14 12:44:37 -0400|docs(skills): add extension-development skill (#23)
Workflow-oriented skill for building Pi extensions. Points the
agent to Pi's live docs and type declarations rather than
duplicating API details that go stale on updates. Includes
component orientation tables and a gotchas section for the
non-obvious mistakes that cause real bugs.
---COMMIT_END---
bc56045ce4d2078aca93b53d5c97726bd18f0293|2026-03-14 12:07:22 -0400|feat(pr-reply): add PR reply mode extension and skill (#22)
New mode for responding to GitHub PR review feedback. The
LLM drives the workflow through a pr_reply tool with actions:
activate, next, review, show, implement, reply, done, skip,
defer, deactivate.

The extension:
- Fetches PR data and review threads via GitHub CLI
- Presents review overviews and thread details in panels
- Tracks thread status (pending, implemented, replied, etc.)
- Coordinates with TDD mode for implementation phases
- Enforces mode boundaries (blocks unrelated tool calls)

The skill teaches the agent the pr-reply workflow methodology:
when to use it, how to analyze threads, and how to write
conversational replies with commit references.
---COMMIT_END---
f805327c1991a8825525c5b9c3a652e387265059|2026-03-14 12:06:28 -0400|feat(skills): add dev-planning skill (#20)
Teaching the agent how to structure implementation plans
that provide context and direction without dictating code.
Covers essential sections (context, interfaces, data flow,
behaviors), what to include vs omit, and plan file naming.
---COMMIT_END---
133448ce5e7cb1cbbd4f85cd74477e84a1e497b6|2026-03-14 12:06:22 -0400|fix(panel): stable height, overflow cap, read-only pages (#19)
Increase PI_CHROME_LINES to 8 to account for extension
widgets that push content past the terminal top.

showPanel now hard-caps output to terminal height, slicing
from the bottom when content overflows.

showPanelSeries gets three fixes:
- Compute content budget from the max option count across
  all pages so height stays constant when switching tabs.
- Pad output to a fixed target height every render.
- Support read-only pages (zero options) with their own
  hint bar and no option key handling.
---COMMIT_END---
83369e221b018da23d46345fbeac93a25d04a658|2026-03-09 21:38:28 -0400|feat(google-workspace): comprehensive improvements to code quality, UX, and authentication (#18)
- Refactor code quality to match best practices
  - Eliminate 59 unsafe type casts with type-safe parameter helpers
  - Extract rendering logic from bloated index.ts (762→258 lines)
  - Split router handlers by domain (Gmail, Calendar, Drive)
  - Add comprehensive JSDoc documentation
  - Extract UI strings and validation to separate modules

- Improve TUI logging with descriptive output
  - Action-specific call rendering (show query, recipients, etc.)
  - Result rendering with compact/expanded views
  - Show sender names, event titles, file types in compact view
  - Color-coded success/error/warning states

- Implement automatic interactive setup
  - Auto-detect missing OAuth credentials
  - Auto-detect missing authentication
  - Guide users through setup without manual commands
  - Persistent storage in Pi session state

- Add dual OAuth flow support
  - Device flow (universal - SSH, containers, remote)
  - Web redirect flow (Desktop app credentials)
  - Automatic fallback between flows
  - Works with both OAuth client types

- Add utility commands
  - /google-setup - Manual OAuth credential setup
  - /google-reset - Clear all configuration

Files changed:
- Modified: index.ts, router.ts, types.ts, auth/credentials.ts, auth/oauth.ts
- Created: params.ts, render-call.ts, render-result.ts, auth-command.ts,
  auth-flow.ts, auth/dual-flow.ts, auth/server.ts, setup-wizard.ts,
  setup-instructions.ts, router/{gmail,calendar,drive}-handlers.ts
---COMMIT_END---
d7a3551568961af2db053392dc94deef6434f33a|2026-03-09 19:17:25 -0400|feat(google-workspace): add Google Workspace extension and skill (#17)
Add comprehensive Google Workspace integration for Gmail, Calendar,
and Drive through a single 'google' tool optimized for AI interaction.

Features:
- Gmail: search, read, send, reply, draft, archive, delete, mark read/unread
- Calendar: view, create, update, delete events, respond to invites
- Drive: search files, open docs/sheets/slides by URL, read comments
- OAuth2 authentication with local callback server
- Editable confirmation gates for sensitive operations
- Multi-account support
- Auto token refresh
- Rich markdown rendering

Architecture:
- router.ts: Routes 18 actions to appropriate handlers
- apis/: Google API clients (gmail, calendar, drive, docs, sheets, slides)
- renderers/: Markdown rendering for each service type
- auth/: OAuth flow, credential storage, callback server
- confirmation.ts: Editable gates using lib/guardian/review-loop

The skill teaches LLM how to translate natural language
("check my email", "schedule meeting with Alice") into
structured tool calls with proper context management.
---COMMIT_END---
16e829646f6172b60e5403a74ab5ca2b85953f72|2026-03-08 23:13:51 -0400|docs: stop enumerating children in higher-level READMEs
Higher-level READMEs that list every extension, skill, or
library module are doomed to drift out of date. Replace
itemized tables and bullet lists with categorical
descriptions that stay accurate as the project grows.

Each extension, skill, and library module already has its
own README — let those be the source of truth for details.

---COMMIT_END---
6cd7683e0fb18f308ced17eb50c1523ce44cffdc|2026-03-08 22:24:10 -0400|fix(panel): dynamic content budget, invalidate, and history guardian renderMarkdown
Three fixes for TUI corruption in scrollable panels:

1. Replace fixed PANEL_FRAME_LINES (7) with PANEL_FRAME_FIXED
   (5) plus the actual option count. Gates with many options
   no longer overflow the terminal.

2. Add invalidate() to both showPanel and showPanelSeries per
   pi's TUI component interface requirement.

3. Render history guardian commands as markdown code blocks
   instead of raw strings. Multi-line commands (backslash
   continuations) were rendered as a single string with
   embedded newlines, causing pi's working indicator to
   bleed into the content. renderMarkdown splits them into
   proper lines with syntax highlighting.

---COMMIT_END---
02fb0d7a73641d3843928f2e89979ff5b9901656|2026-03-08 22:03:59 -0400|feat: render markdown in interview, refactor gate, and transition gate content
Use renderMarkdown for LLM-provided text in the plan
interview (question + context), refactor gate (suggestion
descriptions), and TDD transition gate (summary). This
gives prose proper word wrapping and supports markdown
formatting like headers, lists, and code blocks.

Add "Supports markdown." to the relevant tool parameter
descriptions so the LLM knows formatting is available.

User-typed text (interview answers) stays with plain
wordWrap since it's not markdown.

---COMMIT_END---
042d28c3ef513a74c01fcbbbc58706481e0462ca|2026-03-08 22:00:23 -0400|fix(tdd-mode): wrap transition gate summary with renderMarkdown
The transition gate rendered the summary as a single raw line,
causing long text to overflow into horizontal scroll. Use
renderMarkdown instead — the same renderer guardians use for
prose content. Prose wraps to the visible width, code blocks
don't.

---COMMIT_END---
629963a16905da17011fe8530f3d437c5eca8aa1|2026-03-08 21:54:22 -0400|fix(plan-mode): include own tools in active tool set
When plan mode activates, setActiveTools restricts the LLM
to PLAN_TOOLS. The plan_mode and plan_interview tools were
added in PR #13 but never included in this list, making them
inaccessible during plan mode. Add them.

---COMMIT_END---
796de2a0d0eb801044c72d2df87e0aced54122e6|2026-03-08 21:43:28 -0400|fix(pr-review): make Done the default option on vet done page
The interview and refactor-gate Done pages already had Done
as the first (default-selected) option. The pr-review vet
page had Add a Comment first. Reorder so Done is the default
across all tabbed review panels.

---COMMIT_END---
ead4b99b079be4b5bd8a62c3eed9481a2b2df01a|2026-03-08 21:34:03 -0400|refactor(tdd-mode): drop unused _pi param from restore
Plan-mode's restore uses pi to check the --plan flag, but
TDD's restore never needed it. Remove the phantom parameter
rather than keeping it for false symmetry.

---COMMIT_END---
413532ef9d12b691b1a1ac7cbc98c13d98d6351e|2026-03-08 21:33:55 -0400|refactor(ui): extract shared panel content constants
Move CONTENT_INDENT and FALLBACK_CONTENT_WIDTH to lib/ui/text
and use them in interview, refactor-gate, and pr-review/vet.
Eliminates duplicate definitions and makes content renderers
use the framework-provided width parameter instead of querying
process.stdout.columns directly.

Also extracts SUMMARY_MAX_LENGTH and SUMMARY_MIN_TRUNCATE in
tdd-mode/transitions.

---COMMIT_END---
9019c262fe4498be3907ea6c97c6c4f30434339d|2026-03-08 21:33:42 -0400|fix(tdd-mode): correct README enforcement description
The README prose said test files were blocked in both GREEN
and REFACTOR phases, but the code and enforcement table only
block in GREEN. REFACTOR allows test file writes because
test cleanup is part of refactoring. Fix the prose and JSDoc
to match the actual behavior.

---COMMIT_END---
764a1606cadc1d88b33dcc7f483ae0e01369e297|2026-03-08 21:33:33 -0400|docs(agents): codify design principles from refactor plan
Extract the enduring design principles from the design
refactor plan into AGENTS.md so they guide all future code.

Covers: responsibility-based splitting, composition over
inheritance, guardian pipeline shape, mode file convention,
domain separation, layered library structure, panel
composition model, and idiomatic TypeScript rules.

Updates the Structure section to reflect the current lib/
layout and adds specific prohibitions to What Not to Do.

---COMMIT_END---
4c657861aa358157850dbf3c56f3f1fffad398f4|2026-03-08 21:13:38 -0400|Skill-Driven Mode Activation with LLM-Facing Enforcement (#13)
* feat(plan-mode,tdd-mode): skill-driven activation and
LLM-facing enforcement

Replace command-only activation with LLM-callable tools
that skills guide the agent to invoke naturally. Plan mode
gains a plan_interview tool for tabbed Q&A during planning.
TDD mode shifts from user-facing UI gates to LLM-facing
block hints, with a tdd_refactor tool for tabbed refactoring
review.

Plan mode changes:
- Add plan_mode tool for activate/deactivate
- Add plan_interview tool with tabbed Q&A and looping
- Simplify transition gate (remove TDD option)
- Keep all existing enforcement unchanged

TDD mode changes:
- Add tdd_phase tool for agent-driven state transitions
- Add tdd_refactor tool for tabbed refactoring review
- Replace user-facing gates with LLM-facing block hints
- Remove test-runner regex detection (agent drives phases)
- Remove mandatory refactor gate (replaced by tdd_refactor)
- Confirmation gate on phase transitions with summary
- Custom renderers for session history readability
- Status line shows phial glyph, widget shows test name

Skills updated to teach agents when to call the new tools,
what summaries to provide, and how to loop interviews and
refactoring reviews.

* fix(tdd-mode): allow test file writes in REFACTOR phase

Refactoring tests is valid during REFACTOR — renaming
descriptions, extracting helpers, reordering. Only GREEN
should prevent test modifications.
---COMMIT_END---
5f0121b853c273edbe64dd0414d5061d2d425324|2026-03-08 19:11:55 -0400|style: polish pass across the codebase
Type safety:
- Add isCustomEntry type guard in state.ts, replacing inline
  as casts
- Narrow assistant message type before cast in status-line
- Add safe array access in commit-guardian/validate.ts

Error handling:
- Comment every silent catch block explaining why the error
  is safe to ignore

Import consistency:
- Convert inline require("node:fs") to top-level import in
  pr-review/vet.ts
- Import VirtualConsole from jsdom directly instead of require
- Document intentional lazy require for sqlite3

Shell escaping:
- Replace echo-pipe pattern in pr-review/post.ts with temp
  file approach to prevent corruption on special characters

Code quality:
- Deduplicate bullet/numbered list rendering in content-renderer
  into a single handler with shared indent logic
- Extract named timeout constants in web-search (PAGE_LOAD_TIMEOUT,
  DYNAMIC_CONTENT_WAIT, SEARCH_PAGE_TIMEOUT)
- Add performance note on status-line cost iteration

---COMMIT_END---
c90ed2028dbe938010134cc29634386c0f9647f7|2026-03-08 19:06:53 -0400|refactor(web-search): extract cookie module and fix stderr patching
Split cookies.ts into cookies/ directory with focused modules:
- extract.ts: Chrome profile detection, SQLite queries,
  decryption, domain matching, getCookiesForUrl
- setup.ts: key derivation, caching, StaleKeyError, isSetUp
- inject.ts: puppeteer page cookie injection
- index.ts: barrel export of the public API

browser.ts is now purely Chrome lifecycle with no cookie
knowledge. reader.ts and index.ts import only from the
cookies barrel.

Replace suppressCssWarnings (global process.stderr.write
monkey-patch) with jsdom's virtualConsole option to suppress
CSS parse warnings locally without affecting other output.

---COMMIT_END---
b0a370187a39d58989b2fa6fa6c23227cea2949e|2026-03-08 18:59:36 -0400|refactor(modes): split plan-mode and tdd-mode by responsibility
Both mode extensions are now organized into focused files:

- state.ts: state interface, defaults, constants
- lifecycle.ts: activate, deactivate, toggle, persist, restore
- enforce.ts: tool_call interception and restriction logic
- transitions.ts: agent_end gates, phase advances, context
  injection, stale context filtering
- index.ts: registration only — declares state, wires events

Each index.ts reads as a table of contents for the extension.
No behavior changes.

---COMMIT_END---
dc904564e89b654003386e8c564d5bf972278e1f|2026-03-08 18:53:44 -0400|refactor(guardian): introduce CommandGuardian type and registration
Define CommandGuardian<T> interface with detect/parse/review
pipeline, and registerGuardian helper that wires guardians into
Pi's tool_call event system.

All four guardians now export a CommandGuardian object:
- commitGuardian: CommitParsed (message, flags, prefix)
- prGuardian: PrCommand
- issueGuardian: IssueCommand
- historyGuardian: DestructiveMatch (severity, description)

Command rewriting (event.input.command mutation) is now
centralized in registerGuardian — the single mutation site.
Guardian review functions return { rewrite: string } instead
of mutating directly. Remove onApprove callback from the
review loop.

---COMMIT_END---
e887902536f9b663cc7b3a9773dbe854e5d884a8|2026-03-08 18:51:11 -0400|refactor(guardian): move field editing into field types
Each EditableField now owns its editing logic via edit() and
steerText() methods. Factory functions singleField() and
titleBodyField() encapsulate serialization:

- singleField: opens editor with raw value, updates in place
- titleBodyField: composes # Title + body for editing, parses
  result back into structured fields

The review loop delegates to field.edit() and field.steerText()
without branching on field kind. Removes the no-op content
reassignment line that previously misled readers.

---COMMIT_END---
8ae847eae171b7a8928b5d6b44681744c52d5fe7|2026-03-08 18:43:15 -0400|refactor(ui): consolidate wordWrap into lib/ui/text
Create lib/ui/text.ts with the paragraph-aware wordWrap
implementation. Remove duplicates from content-renderer and
pr-review/vet.ts. Both now import from the shared module.

---COMMIT_END---
16dde94aec1a3432584bbaae23f28f0e324a4f9c|2026-03-08 18:41:56 -0400|refactor(ui): move showContent to content-viewer
Move showContent from content-renderer into content-viewer,
its only consumer. content-renderer becomes a pure rendering
module with no UI or panel dependencies.

---COMMIT_END---
0f4e81c5a7cd002ffcb24085d5021721656dd127|2026-03-08 18:40:23 -0400|refactor(ui): decompose panel into composable primitives
Extract from panel.ts (725 lines) into three focused modules:

- panel-state.ts: constants, layout budget calculation, scroll
  clamping
- panel-render.ts: pure rendering functions for viewport,
  options, editor, scrollbar, tab bar, and ANSI-aware
  horizontal slicing
- panel-keys.ts: input processors for scroll, option
  navigation, tab navigation, and selection confirmation

Both showPanel and showPanelSeries now compose these primitives
while keeping their own orchestration and state management.
Public signatures and behavior unchanged.

---COMMIT_END---
13640ecb82b0c6b99160cc10be86f14c8ff91656|2026-03-08 18:36:56 -0400|refactor(lib): organize into ui/, guardian/, parse/ layers
Create subdirectory structure within extensions/lib/:
- ui/ — panel, gate, content-renderer
- guardian/ — review-loop
- parse/ — command (shell-level), gh-command (gh CLI domain)
- state.ts stays at top level

Split command-parse.ts into two modules by domain: shell-level
parsing (flag extraction, heredoc, quoting, splitting) and
gh CLI domain (command detection, rebuilding, multi-flags).

Update all import paths across the codebase.

---COMMIT_END---
ca1ac88446c769bf1a581dfa8241af34517415f8|2026-03-08 18:34:20 -0400|refactor(lib): rename shared/ to lib/
Move extensions/shared/ to extensions/lib/ and update all
import paths across the codebase. Update documentation
references in AGENTS.md, extensions/README.md, and the
lib README.

Pure move — no behavior changes.

---COMMIT_END---
8e5a5f903b4c98d718384562ea8dc6e072d9014b|2026-03-08 17:43:00 -0400|chore: add Biome linter and fix all lint issues
Add Biome as the project's linter and formatter. Fix every
existing lint issue across all 33 extension files rather
than suppressing or downgrading rules.

Categories of fixes applied:
- noNonNullAssertion: replaced ! with ?? fallbacks, guard
  clauses, or optional chaining
- noExplicitAny: replaced any with proper types (Theme,
  CookieRow, { preApproved?: boolean })
- noAssignInExpressions: rewrote while/exec loops to use
  for...of with matchAll
- useTemplate: converted string concatenation to template
  literals
- useOptionalChain: replaced verbose null checks with ?.
- noUnusedVariables: removed unused STATUS_ICONS constant
- noUselessSwitchCase: removed redundant case before default
- Formatting: import ordering, line wrapping

AGENTS.md now instructs agents to run the linter after code
changes and before committing.

---COMMIT_END---
bc95fea24e40c7a0f47e46f07f054a1abd0d2693|2026-03-08 17:27:12 -0400|feat(panel): show selected option icon in tab bar (#10)
Tab bar previously showed ■/□ for answered/unanswered tabs,
giving no indication of which option was chosen. Now each
PanelOption can declare an icon, and the tab bar displays it
once that option is selected. Falls back to ✓ when no icon
is specified.

PR review tabs show ✓/✗/✎/⚡ for approve/reject/edit/steer.
Ask tool shows ✎ for free-form "Type something" responses.
---COMMIT_END---
3c1eb08d8fec6815148d21f882472f02f4ee78e2|2026-03-08 17:21:24 -0400|Add Theme-Aware Syntax Highlighting to Panel Renderer (#9)
* feat(renderer): add syntax highlighting to panels

Use pi's built-in highlightCode and getLanguageFromPath to
syntax-highlight code fences in markdown, code file views,
and PR review comment snippets. Diff fences render with
toolDiffAdded/toolDiffRemoved/toolDiffContext theme tokens.

All colors flow through pi's theme system, so users can
customize syntax highlighting via their theme JSON.

* fix(renderer): cap word wrap to terminal width

Pi-tui passes a render width far larger than the physical
terminal (e.g. 398 on an 80-column terminal). This caused
prose in renderMarkdown and PR review comment bodies to
never word-wrap, requiring horizontal scrolling to read.

Cap wrap width to process.stdout.columns so prose wraps
to the visible terminal while code and diffs remain free
to extend for horizontal scrolling.

* fix(pr-review): remove duplicate status icon in tab bar

The vet page label included a status icon (□/✓/✗/✎) but
the panel's renderTabBar already prepends its own checkbox.
This caused double boxes in the tab bar. Remove the icon
from the label and let the tab bar handle it.
---COMMIT_END---
8e44c660eb5604a61c29040e562cde03d9a2baa2|2026-03-08 16:46:59 -0400|Reorganize Skills and Extensions for Consistency and Composability (#7)
* feat(shared): add content renderer for markdown, diffs, and code

Themed rendering functions that turn raw text into display-ready
lines for TUI components. Three renderers:

- renderMarkdown: headers, blockquotes, lists, code fences,
  bold/italic, and word-wrapping
- renderDiff: unified diff coloring (+green, -red, @@cyan)
- renderCode: line numbers with gutter and highlight support

Also includes content type auto-detection from text or file
path, and a renderContent convenience dispatcher.

This is the bottom layer of the shared UI primitive stack.
Panels, gates, and the standalone viewer will compose on it.

* feat(shared): add panel primitive with single-shot and series modes

Two public APIs sharing the same rendering internals:

- showPanel: single-page bordered panel with scrollable content,
  numbered options with descriptions, and inline editor. Returns
  on selection or cancel. Used by gate and guardians.

- showPanelSeries: multi-page panel with tab bar navigation.
  Users can browse tabs freely and answer in any order. Async
  onSelect callback supports full-screen editor overlays via
  pi's overlay system. Resolves when onSelect returns true.

Shared internals ported from gate: scrollbar with viewport,
content budget calculation, editor theme, and keyboard handling
(arrows, enter, escape, shift+arrows for scrolling, tab/left/
right for page navigation).

* refactor(shared): rewrite gate as thin wrapper over panel

Gate now delegates all rendering, scrolling, keyboard handling,
and editor management to showPanel. The public API is unchanged
— showGate, formatSteer, GateOption, GateConfig, and GateResult
all keep their existing signatures.

Gate adds one behavior on top of panel: auto-appending a "Steer"
option with opensEditor and mapping the panel result back to the
GateResult shape with a feedback field.

All gate consumers (guardians, tdd-mode, plan-mode, pr-review)
gain panel improvements — scrolling, option descriptions —
without code changes.

* refactor(ask): compose on showPanelSeries instead of raw TUI

Ask no longer manages its own Editor, keyboard handling, tab
bar rendering, or option display. It builds PanelPages from
questions and delegates all UI to showPanelSeries.

Each question maps to a PanelPage with its prompt as content
and options as panel options. "Type something" uses opensEditor.
Multi-question mode adds a Submit page showing the answer
summary. Single-question mode resolves on first selection.

Scrolling is now available for questions with long content or
many options (previously missing). The renderCall and
renderResult methods are unchanged.

* refactor(pr-review): tabbed comment vetting via showPanelSeries

Replace the sequential one-gate-per-comment flow with a tabbed
panel where each comment is a page. Users can freely navigate
between comments, approve/edit/reject in any order, and submit
when done.

Key changes:
- Each comment tab shows status indicators (✓ ✗ ✎ □)
- Edit opens ctx.ui.editor as a full-screen overlay
- "Add comment" uses inline editor on the Done page
- Steer aborts the entire review with feedback
- Code snippets now use renderCode from content-renderer

The VetResult interface and all calling code in index.ts are
unchanged.

* feat(shared): add showContent standalone viewer

Wraps showPanel with rendered content and a single Close option.
Auto-detects content type (markdown, diff, code) or accepts an
explicit type. Optional title shown as a bold header.

Programmatic API for any extension that wants to present
scrollable formatted content without building its own viewer.

* feat(content-viewer): add /view command for scrollable file viewing

Thin extension that registers the /view command and delegates
to showContent from shared/content-renderer. Auto-detects
content type from file extension — markdown gets themed
headers and lists, diffs get +/- coloring, code gets line
numbers.

Usage: /view path/to/file

* feat(commit-guardian): extract commit review from git-guardian

Move commit message review into its own extension. Files are
copied from git-guardian with the index.ts trimmed to only
handle git commit commands. Review, validation, and parsing
code is unchanged.

Pairs with the git-commit-format skill.

* feat(history-guardian): extract destructive command protection

Move destructive/history-rewriting command protection into its
own extension. Renamed destructive.ts to confirm.ts since the
extension name already communicates what it guards.

Catches: force-push, hard reset, rebase, clean, branch -D,
checkout --, and stash drop. Each with risky vs irrecoverable
severity.

* refactor: remove git-guardian after split

git-guardian's functionality is now in commit-guardian
(commit message review) and history-guardian (destructive
command protection).

* fix(shared): add horizontal scrolling and overlay mode to panels

Instead of truncating long lines (losing content), panels now
support horizontal scrolling via Shift+←→. An ANSI-aware
horizontalSlice function preserves escape code state across
the scroll boundary.

Both showPanel and showPanelSeries support:
- Horizontal scroll (Shift+←→) with 20-char step
- Overlay mode ({ overlay: true }) for floating panels
- Hint line adapts to show available scroll directions

showContent uses overlay mode so /view floats on top of the
conversation. Content renderers are no longer constrained to
terminal width — the panel handles overflow via scrolling.

* fix(shared): add defensive truncation after horizontal slice

The TUI crashes if any rendered line exceeds terminal width.
Add explicit truncateToWidth calls after horizontalSlice in
renderScrollableContent as a safety net — even though
horizontalSlice already calls truncateToWidth internally,
ANSI escape sequences can sometimes cause visible width
miscalculation.

Also truncate the showContent title line which bypasses the
scrollable content path.

* fix(shared): use padding instead of CSI positioning for scrollbar

The CSI absolute column positioning (\x1b[nG) breaks in overlay
mode — the TUI composites overlay lines onto footer lines,
causing the absolute position to land in the wrong place and
the combined line to exceed terminal width.

Replace with explicit space padding to contentWidth, then append
the scrollbar character. This is overlay-safe and produces
correct visible width.

* fix(shared): remove overlay mode, panels render inline

The TUI's overlay compositing concatenates overlay lines with
footer content, causing combined width to exceed the terminal.
This is a fundamental incompatibility with how we render panels.

Remove overlay support entirely. Panels render inline (same as
gates always did). The /view command still works — it renders
inline with scrolling. Horizontal scrolling via Shift+←→
remains for long lines.

The overlay API needs further investigation before it can be
used with full-width panel content.

* fix(shared): restore CSI positioning for scrollbar in inline mode

The padding approach caused inconsistent scrollbar alignment
because visibleWidth on ANSI strings didn't always match the
terminal's rendering.

Restore the original CSI absolute column positioning
(\x1b[nG) which works correctly in inline mode. The earlier
overlay crash was specific to overlay compositing — since
we've removed overlay mode, CSI positioning is safe again.

* docs(shared): minor JSDoc cleanup in panel header

* feat(shared): add generic review loop for guardian flows

Parameterized approve/edit/steer/reject cycle built on gate.
Supports three patterns:

- Single field editing (commit messages)
- Title+body editing with # header split (PRs, issues)
- No editing, just allow/block (history-guardian)

The loop handles the gate interaction, full-screen editor
overlay for edits, steer feedback formatting, and cancel/
reject messaging. Guardians provide content renderer, actions,
and an onApprove callback for command rewriting.

* refactor(commit-guardian): use shared review loop

Replace the hand-rolled gate loop with reviewLoop. Commit-
specific rendering (validation indicators, amend warning)
stays in validate.ts and plugs into the content renderer.
Command rewriting moves to the onApprove callback.

* refactor(pr-guardian): use shared review loop with renderMarkdown

Replace the hand-rolled gate loop and ad-hoc markdown coloring
with reviewLoop and renderMarkdown from shared modules. PR body
now gets proper themed headers, lists, blockquotes, and code
fences instead of the simple per-line if/else chain.

* refactor(issue-guardian): use shared review loop with renderMarkdown

Same treatment as pr-guardian — replace the hand-rolled gate
loop and ad-hoc markdown coloring with reviewLoop and
renderMarkdown. The only difference from pr-guardian is the
entity name ("issue") and header labels.

* refactor(history-guardian): use shared review loop

Replace the hand-rolled gate call with reviewLoop using
allow/block actions, no editable field, and no command
rebuilder. The content renderer and steer context are
unchanged.

* feat(shared): add command parsing utilities for guardians

Shared functions extracted from the nearly-identical parse.ts
files in pr-guardian and issue-guardian:

- extractFlag, extractBody, quote: shell argument parsing
- splitAtCommand: generalized prefix splitting
- extractEntityNumber: PR/issue number from edit commands
- isGhCommand: gh subcommand detection
- rebuildGhCommand: heredoc command reconstruction
- extractMultiFlags: repeated flags (--label, --assignee)

Also usable by commit-guardian for splitAtCommand.

* refactor: deduplicate command parsing across guardians

Extract shared parsing functions (extractFlag, extractBody,
quote, splitAtCommand, extractEntityNumber, rebuildGhCommand,
extractMultiFlags) into shared/command-parse.ts.

PR and issue guardian parse.ts files now import shared
functions and keep only their entity-specific extra-flag
extraction. Commit guardian uses splitAtCommand for prefix
splitting while keeping its git-specific message extraction.

* docs(shared): rewrap content-renderer JSDoc header

* refactor: rename skills for consistency

Rename four skills to establish clear naming patterns:

- planning → plan-workflow (aligns with plan-mode extension)
- conventional-commits → git-commit-format (git- prefix family)
- branch-management → git-branch-management (git- prefix family)
- rebase-resolution → git-rebase-resolution (git- prefix family)

git-hygiene and tdd-workflow already follow their respective
patterns and are unchanged. Updated plan-mode README link to
point to the renamed plan-workflow skill directory.

* feat: add CLI conventions skills and trim domain skills

Create two new skills:
- git-cli-conventions: heredoc syntax for git commit, shell
  quoting, and command patterns
- gh-cli-conventions: heredoc syntax for gh commands, Title
  Case rules, and metadata in separate commands

Trim CLI-mechanical content from domain skills:
- pr-writing: remove Creating PRs via CLI section, reference
  gh-cli-conventions
- issue-writing: remove Creating/Editing via CLI sections,
  reference gh-cli-conventions
- git-commit-format: remove heredoc example block, reference
  git-cli-conventions

Each domain skill now focuses on content and narrative. CLI
formatting mechanics live in the conventions skills, loadable
independently whenever the agent needs command syntax.

* docs(extensions): document taxonomy and shared utilities

Rewrite extensions/README.md with four categories: guardians
(intercept actions), modes (stateful enforcement), tools (new
capabilities), and UI (display components). Each category has
a table listing its extensions.

Update shared/README.md to describe all six shared modules:
content-renderer, panel, gate, review-loop, command-parse,
and state.

* docs: update README with pairing table and fix stale references

Add skill ↔ extension pairing table to the top-level README.
Update the composability example to use new names
(commit-guardian, git-commit-format). List unpaired skills
and extensions.

Fix stale "git-guardian" reference in tdd-mode/README.md →
"commit-guardian".

* docs(skills): add no-hard-wrap guidance for PR and issue bodies

GitHub's markdown renderer reflows paragraphs — hard line
breaks within a paragraph render as visible breaks, making
text choppy. Add explicit guidance to gh-cli-conventions,
pr-writing, and issue-writing: write each paragraph as a
single continuous line. Hard-wrapping at 72 chars is for
commit messages only.

* fix(shared): word-wrap all markdown content types in renderMarkdown

Headers, blockquotes, list items, and numbered lists were
truncated at terminal width instead of word-wrapped. Long
single-line paragraphs (as recommended for PR/issue bodies)
would force horizontal scrolling in the gate panel.

Now all non-code-block content word-wraps to fit the panel
width. List continuation lines are indented to align with
the first line's text. Code blocks and diffs remain unwrapped
since horizontal scrolling is correct for those.
---COMMIT_END---
f6a159dd0e87f5f7b63ddc91423278f095dcc109|2026-03-08 14:18:21 -0400|feat: add issue guardian extension and issue writing skill
Gate gh issue create and gh issue edit commands with the same
approve/edit/steer/reject review flow used for commits and
PRs. Paired with a skill that teaches heredoc CLI format for
reliable special character handling.

---COMMIT_END---
8f9b8d10d58d643bc1347dafdacbe4ff806f211d|2026-03-08 01:01:31 -0500|feat(gate): add visual scrollbar for scrollable content
Renders a scrollbar track on the right edge of the viewport
when content exceeds the terminal height. Uses ANSI cursor
positioning to align the scrollbar column absolutely, avoiding
misalignment from emoji and wide characters.

---COMMIT_END---
d229fbdafda3eb741439d369192f2ba4ad0d5afa|2026-03-08 00:54:48 -0500|feat: add PR guardian extension for description review (#4)
Gates gh pr create and gh pr edit commands with a review
dialog showing the formatted PR title and body. Supports
approve, edit, steer, and reject — matching the commit
review gate UX from git-guardian.

Parses both --body and --body-file heredoc formats, preserves
original flags (--draft, --base, labels, assignees, etc.),
and rebuilds the command when the user edits content.
---COMMIT_END---
2c7e2bd6b27b41a99d81a342e6e0865ea8a1c2e3|2026-03-08 00:54:45 -0500|feat(web-search): inject Chrome cookies for authenticated pages (#3)
web_read can now access pages behind authentication (e.g. Google
IAP, SSO) by reading and decrypting cookies from the user's
local Chrome profile.

A new /setup-chrome-cookies slash command caches the Chrome Safe
Storage decryption key to ~/.config/pi/chrome-cookie-key. This
is the only step that triggers a macOS Keychain prompt, keeping
it explicit and user-initiated. On Linux no setup is needed.

Auth redirect detection guides users to run the setup command
when they hit a login page. Stale key detection (e.g. after
Chrome reinstall) suggests /setup-chrome-cookies --force.

Also makes JSDOM/Readability failures non-fatal by falling back
to puppeteer's own text extraction for pages with problematic
CSS.
---COMMIT_END---
2b99081dc7861a47f21c0140836494d4c04d9478|2026-03-08 00:54:41 -0500|feat(gate): add scrolling for content that exceeds terminal (#2)
When gate content is taller than the available terminal space,
the viewport is capped and scroll indicators appear. Shift+↑↓
and PgUp/PgDn scroll the content while ↑↓ still navigates
options. Prevents the gate from corrupting terminal scroll
history on long PR descriptions or commit messages.
---COMMIT_END---
bebb75c848eae3b8d97deaa633e26c19e6601b70|2026-03-08 00:47:44 -0500|revert: undo squash merge of PR #1
Reverting to re-land as two separate PRs: one for Chrome
cookie injection, one for the PR guardian extension.

---COMMIT_END---
dcbe0effba3981792ba3b49d8b18d79cf44d0ca1|2026-03-08 00:46:27 -0500|Enable web_read Access to Authenticated Pages via Chrome Cookies (#1)
* feat(web-search): inject Chrome cookies for authenticated pages

web_read can now access pages behind authentication (e.g. Google
IAP, SSO) by reading and decrypting cookies from the user's
local Chrome profile.

A new /setup-chrome-cookies slash command caches the Chrome Safe
Storage decryption key to ~/.config/pi/chrome-cookie-key. This
is the only step that triggers a macOS Keychain prompt, keeping
it explicit and user-initiated. On Linux no setup is needed.

Auth redirect detection guides users to run the setup command
when they hit a login page. Stale key detection (e.g. after
Chrome reinstall) suggests /setup-chrome-cookies --force.

Also makes JSDOM/Readability failures non-fatal by falling back
to puppeteer's own text extraction for pages with problematic
CSS.

* feat: add PR guardian extension for description review

Gates gh pr create and gh pr edit commands with a review
dialog showing the formatted PR title and body. Supports
approve, edit, steer, and reject — matching the commit
review gate UX from git-guardian.

Parses both --body and --body-file heredoc formats, preserves
original flags (--draft, --base, labels, assignees, etc.),
and rebuilds the command when the user edits content.
---COMMIT_END---
4daac39e34943ff038e6b3a06eb4be6948103941|2026-03-07 23:43:55 -0500|feat: show page excerpt in web_read render result
Display the article excerpt below the title and char count in
the compact tool result view, giving quick feedback about what
a page contains.

---COMMIT_END---
3782277ca4c9bc395972f8d59e80affbde551c52|2026-03-07 23:36:24 -0500|fix: reduce web-search token usage and terminal noise
Strip junk DOM elements (ads, nav, cookies, comments, social
buttons, related articles) via CSS selectors before Readability.
Remove boilerplate lines after extraction. Collapse whitespace.

Large pages (>12k chars) are saved to /tmp/pi-web-read/ and the
agent gets a summary + file path to explore selectively with
read/grep instead of consuming everything as tokens.

Add compact renderCall/renderResult for both tools. Suppress
Chrome stderr and jsdom CSS warnings.

---COMMIT_END---
2175eb98ba9d9b1362b74ef42ae4cadc0dc58762|2026-03-07 23:23:09 -0500|feat: add web-search extension with headless Chrome
Two tools for the LLM: web_search (DuckDuckGo via headless
Chrome) and web_read (fetch + Mozilla Readability extraction).

Uses puppeteer-core with the system Chrome install, jsdom, and
@mozilla/readability. Browser instance is reused across calls
and closed on session end.

Also fixes AGENTS.md dependency guidance — third-party deps
are fine in package.json, only pi's own packages are excluded.

---COMMIT_END---
61bb80b5455b58ac86d41cce99b3da787d14ca45|2026-03-07 22:58:10 -0500|fix: tweak status-line styling and segment order
Move thinking glyph to far right, dim model name to match
surrounding text.

---COMMIT_END---
8df80d50107135e25b1f34c5629b3a18ab673166|2026-03-07 22:55:39 -0500|docs: rewrite AGENTS.md with repo-specific context
Replace generic collaboration preferences with practical info
an AI agent needs: what this project is, structure, conventions,
testing workflow, and concrete guardrails.

---COMMIT_END---
0f01d977a8fdb6f21098207c4aa9c9d6de598854|2026-03-07 22:47:42 -0500|feat: add responsive status-line footer extension
Single-line footer that right-justifies context info and
progressively degrades as the terminal narrows: shrink dir,
drop cost, tokens to percent, shrink model, drop thinking
glyph, drop branch. Basename is always shown.

---COMMIT_END---
9b59aa4244f6b7b181b3ca46a81dd451f4b0440f|2026-03-07 22:28:49 -0500|docs: distribute documentation into per-component READMEs
Slim down the top-level README to an overview of how skills and
extensions work together. Add READMEs to each extension and skill
subdirectory so details live next to the code they describe.

Skip skills/README.md since pi treats root-level .md files in
the skills directory as skills.

---COMMIT_END---
7cd0876033f83fde14ffdd7b80b3a559389ed0fd|2026-03-07 22:23:19 -0500|chore: add MIT license file

---COMMIT_END---
38075646f8d87c6d0bd4d9837da02b139ed6bf85|2026-03-07 22:22:49 -0500|docs: rewrite README for new repo name
Update package name to agentic-harness.pi, add real install
URL, include pr-review in extensions table, and rewrite for
a consumer audience following the style of dotfiles and
core.nix READMEs.

---COMMIT_END---
75ea9c11581bbfb5f0ea4f0c3322a0c60b92edbe|2026-03-07 22:18:15 -0500|feat(pr-review): add self-review extension
Interactive tool that lets the LLM propose inline review
comments on a PR, then walks the user through each one
via the shared gate (approve, edit, reject, steer). Supports
multi-round flows where the user can add comments via
natural language for the LLM to resolve into structured
review comments. Posts approved comments as a single GitHub
PR review via gh api.

---COMMIT_END---
c864c987793797d692961a643f41dc8caa46c781|2026-03-07 22:18:08 -0500|docs: refine commit, PR, and TDD skill guidance
Conventional-commits skill now warns against describing
future work or parroting implementation details. PR-writing
skill adds issue/stack context, self-review via pr_review
tool, CLI examples, and emoji section headers. TDD-workflow
skill adds test ordering (happy path → edge cases → errors).

---COMMIT_END---
b2947cf60c7e5e227a41a3c677b34f44eb605029|2026-03-02 12:52:32 -0500|feat: add github-sub-issues and github-projects skills
GraphQL-based skills for managing GitHub Issues parent/child
relationships and Projects v2 boards. Each includes a SKILL.md
and helper scripts for querying, reordering, and resolving IDs.

---COMMIT_END---
31ca9db49540362949a6679fab24277f5fb9ecf0|2026-02-27 17:06:14 -0500|refactor(tdd-mode): simplify phases and extract patterns
Collapse five TDD phases to three (red/green/refactor) — the
run_red and run_green intermediaries added ceremony without value
since the agent runs tests naturally within each phase.

Extract test file and test runner detection into patterns.ts.
Deduplicate overlapping regexes and split the monolithic runner
regex into individual readable patterns.

Pull activate/deactivate/toggle helpers out of the command and
shortcut handlers. Remove dead ranTests variable. Use
isBashToolResult() instead of unsafe cast.

---COMMIT_END---
a08da7dc8db98a5562e791d57deaecb904e16907|2026-02-27 17:02:20 -0500|refactor(plan-mode): extract toggle, snapshot tools
Deduplicate command/shortcut toggle logic into toggle().
Save active tools before restricting and restore on
deactivate instead of hardcoding NORMAL_TOOLS.
Simplify transition gate and state persistence.

---COMMIT_END---
f582747e758932f8fe899f7eb35ccef11d19bc55|2026-02-27 17:01:10 -0500|refactor(git-guardian): split into focused modules
Extract five single-responsibility modules from the
monolithic 400-line file:

- parse.ts: message extraction, command splitting, heredoc
- validate.ts: commit validation and content rendering
- patterns.ts: destructive command pattern table
- review.ts: commit review loop
- destructive.ts: destructive command gate

Entry point is now a 30-line router. Fix splitAtCommit to
use greedy match (splits at last separator, not first).

---COMMIT_END---
a11f19a1d45e7e70f47e2b8c3ea46c10f1c988e7|2026-02-27 16:59:12 -0500|refactor(gate): simplify component, use Theme type
Remove render caching (invalidated every input, never hit),
unused steerIndex, and non-interface invalidate method.

Use pi's Theme type for content callbacks. Extract
renderOptions, renderSteer, buildEditorTheme as focused
helpers. Number keys select directly without fall-through.

---COMMIT_END---
b17fada34d960d3b7778216d323bf0cf0b51ad93|2026-02-27 16:57:05 -0500|refactor: extract shared state helpers, static imports
Add shared/state.ts with getLastEntry() and filterContext()
to replace duplicated patterns in plan-mode and tdd-mode.

Switch all extensions from dynamic to static imports now
that we've confirmed jiti handles them fine. Delete the
duplicate git-guardian/gate.ts.

---COMMIT_END---
3c22c7fcbc29878afe0a6e3b3166e84982a00c40|2026-02-27 15:55:42 -0500|refactor: unify UX with shared gate component
Replace ctx.ui.select() across all extensions with a shared
gate component (extensions/shared/gate.ts) using ctx.ui.custom()
for consistent visual language: accent borders, numbered options,
and an inline steer editor on every prompt.

Key changes:
- Shared gate in extensions/shared/gate.ts
- git-guardian: commit validation indicators, dynamic gate import,
  approve path lets original command run (normal-colored output),
  edit path rewrites command before execution
- plan-mode: setActiveTools(), Ctrl+Alt+P, --plan flag fix
- tdd-mode: RED-phase file enforcement, Ctrl+Alt+T
- Rename questionnaire to ask (extensions/ask/)
- Update README, skills, remove empty prompts dir

---COMMIT_END---
b603df3316543c3350813e766548c90af1169de2|2026-02-27 15:20:44 -0500|feat(skills): add branch, rebase, and PR skills
Port the principles from Claude Code's branch-builder,
rebase-resolver, and pr-builder agents into Pi's skill
format. Teaches naming conventions, conflict resolution
philosophy, and PR narrative structure without the
orchestrator machinery or Shopify-specific details.

---COMMIT_END---
4a0cdbc12711b5c5ed328e6ca732882061c742ea|2026-02-27 15:09:02 -0500|docs: usage guide and project-local override examples
Full README covering installation, all extensions and skills,
usage flows for planning, TDD, committing, and research.
Documents composability, project-local overrides, and commands.

---COMMIT_END---
4448aab9380b020cc451b2ad7c8e5a46e12078eb|2026-02-27 15:08:19 -0500|feat(tdd-mode): red-green-refactor with phase enforcement
State machine tracks red/green/refactor phases by observing
test execution results. Refactor gate pauses for user input
with options to refactor test, implementation, or commit.
Commit proposals flow through git-guardian for review. Accepts
optional plan file for step tracking.

---COMMIT_END---
721cf43f7106c950b434a99178e377ae5f1139e9|2026-02-27 15:07:17 -0500|feat(plan-mode): read-only investigation with plan writing
Toggle via /plan command or --plan flag. Writes restricted to
configurable plan directory (default .pi/plans/). Destructive
bash commands blocked via blocklist. Offers transition to TDD
or free-form implementation after plan file is written.

---COMMIT_END---
4e1539743dad9510e823cb3547d8f62ff7e4c47b|2026-02-27 15:04:14 -0500|feat(skills): planning and TDD workflow methodology
Planning skill covers collaborative investigation flow, plan
file format, step granularity, and references /plan command.
TDD skill covers red-green-refactor cycle, failure validation,
stubbing strategy, commit cadence, and test discovery.

---COMMIT_END---
4b3c8c1a6bd293d1e08bdeedbc33e609397d79a1|2026-02-27 15:03:34 -0500|feat(skills): code investigation methodology
Guides research-first behavior: map the area, read code,
summarize with focus on what matters, pause for feedback
before proposing changes.

---COMMIT_END---
7432c75caa38aba0e69d8596702b3295375b13c5|2026-02-27 15:03:19 -0500|feat(skills): conventional commits and git hygiene
Conventional-commits skill covers the full spec with heredoc
format, 50/72 rules, type reference, and what+why guidance.
Git-hygiene skill covers commit frequency, atomic commits,
and history management practices.

---COMMIT_END---
4d3b426aee81f868502966ef68b44312735802eb|2026-02-27 15:02:40 -0500|feat(questionnaire): reusable structured question tool
Standalone tool for asking single or multi-question flows
with options and free-form input. Available for any skill
or extension that needs structured user input.

---COMMIT_END---
bab040759864dc65f5b94efaf6c6e65e03444d4f|2026-02-27 15:00:11 -0500|feat(git-guardian): commit review and destructive cmd protection
Intercept git commit for message review with approve, steer
(natural language feedback), edit, or reject. All commits
normalize to heredoc format on execution. Destructive git
commands get severity-appropriate confirmation with the same
steer option. Validation left to the conventional-commits
skill — the guardian just holds the door.

---COMMIT_END---
62dd9399b8ec09059f2427520edd212613ea332e|2026-02-27 14:46:32 -0500|chore: scaffold pi-setup package structure
Pi package with extension and skill directories, minimal
AGENTS.md for collaboration style, and README documenting
the planned components.

---COMMIT_END---