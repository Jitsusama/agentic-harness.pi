/**
 * Repository discovery and directory switching for PR review.
 *
 * Finds the target repo on disk and handles switching when
 * the PR belongs to a different repo than the current cwd.
 *
 * Stubbed — implemented in M3.
 */
