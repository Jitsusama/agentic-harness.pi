# Ask Extension

A structured question tool the agent uses to gather input during
planning, investigation, or any workflow that needs clarification.

## How It Works

The agent calls the `ask` tool with one or more questions, each
with predefined options. The user picks from the options or types
a free-form response. Multiple questions are navigated via a
tab bar.

Any skill or extension can rely on this tool being available.
