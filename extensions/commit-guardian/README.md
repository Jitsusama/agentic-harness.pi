# Commit Guardian Extension

Intercepts `git commit` commands and presents the commit message
for review before execution.

## What It Does

Every `git commit` is intercepted. You see the message with
validation indicators (subject length, body wrap, conventional
format) and can approve, edit, steer, or reject.
