# Panel Zoom Widget

Global shortcuts for toggling panel height between minimized,
normal and fullscreen modes.

- **Ctrl+Alt+F**: toggle fullscreen (normal ↔ fullscreen)
- **Ctrl+Alt+M**: toggle minimized (normal ↔ minimized)

The status-line extension reads the current mode glyph from
the shared panel height module and renders it as a system
indicator.
